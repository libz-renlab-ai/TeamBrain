```text
   PR-PLAN ──► 2 file edits ──► §V1 RUN ──► 6/6 PASS ──► push to PR #191
```

# Fix-report — PR #191 doc gap on GitHub App install

Companion to `docs/plans/2026-05-09-pr-191-fix-plan.md`.

## Trigger

Cloud `claude-review` GH Action on PR #191 returned 401 Unauthorized:
"Claude Code is not installed on this repository. Please install the
Claude Code GitHub App at https://github.com/apps/claude". This exposed
a doc gap in `docs/features/claude-code-action.md` — the original wording
named the `CLAUDE_CODE_OAUTH_TOKEN` secret but not the GitHub App half
of the install.

## What shipped

| File | Change | Net lines |
|---|---|---|
| `docs/features/claude-code-action.md` | added "Prerequisites — both halves of the install" near the top + "Troubleshooting" section near the end | +48 |
| `docs/plans/2026-05-09-docs-install-github-app-report.md` | appended "Post-PR observation — fix-cycle 1" section recording the live 401 as the trigger | +30 |
| `docs/plans/2026-05-09-pr-191-fix-plan.md` | new | (the PR-PLAN itself) |
| `docs/plans/2026-05-09-pr-191-fix-report.md` | new | (this file) |

No edits to `.github/workflows/`, `docs/adr/0007-*.md`, source code, or
`scripts/*.sh`.

## Judge harness §V1 RUN — results

Run from the worktree at `2026-05-09T14:50 +08`:

| # | Check | Result |
|---|---|---|
| 1 | `wc -l docs/features/claude-code-action.md` < 200 | PASS — 196 |
| 2 | `grep -c 'github.com/apps/claude' docs/features/claude-code-action.md` ≥ 1 | PASS — 4 |
| 3 | `grep -c 'App token exchange failed' docs/features/claude-code-action.md` ≥ 1 | PASS — 1 |
| 4 | `grep -c -i 'GitHub App' docs/features/claude-code-action.md` ≥ 2 | PASS — 6 |
| 5 | No edits to `.github/`, `docs/adr/`, `packages/`, `scripts/` | PASS — `git diff --name-only origin/main..HEAD -- '.github/' 'docs/adr/' 'packages/' 'scripts/'` empty |
| 6 | `git diff origin/main..HEAD --stat` matches "files changed" | PASS |

## Open / blocking concerns

- The cloud `claude-review` GH Action will keep failing on every PR
  (this one and future ones) until a human completes the GitHub App
  install at `https://github.com/apps/claude`. **Not blocking for this
  PR's merge** per ADR-0007 — the local `/review` skill is the
  authoritative gate and is unaffected.
- The Codex bot (chatgpt-codex-connector[bot]) is still attached at the
  org level (separate cleanup, out of scope for this PR) and commented
  "You have reached your Codex usage limits". Same: not blocking.

## Next step

Push the fix-cycle commits to the same `docs/install-github-app` branch
(no force-push, no rebase). Then re-run local `/review` on the cumulative
diff and the §V6 / §V8 `claudefast` probes from the fix-PR-PLAN. If
`/review` returns PASS, the POSTPR loop terminates and the PR is ready
for human merge decision (squash-only per project convention).
