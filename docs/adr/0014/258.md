---
Issue: "#258"
Title: [fixedflow] 清理 PR #257 后剩余 3 处 superpowers 残留 
State: CLOSED
Closed-at: 2026-05-10T02:56:41Z
Closing-PRs: #259
Saved-at: 2026-05-12
Saved-by: maintainer backfill (post-ADR-0014 acceptance)
Source-URL: https://github.com/libz-renlab-ai/TeamBrain/issues/258
---

# Grill log: issue #258

> Saved verbatim from GitHub comments per ADR-0014 §"Operational shape" step 3.
> Save-mode backfill: issue pre-existed before ADR-0014 was accepted (2026-05-11),
> so this file is written from existing GitHub grill comments — no new grilling.

## Issue metadata

- **Title**: [fixedflow] 清理 PR #257 后剩余 3 处 superpowers 残留
- **State**: CLOSED
- **Closed at**: 2026-05-10T02:56:41Z
- **Closing / related PRs**: #259
- **Source URL**: https://github.com/libz-renlab-ai/TeamBrain/issues/258

## Grill comments (verbatim)

### Comment 1 — @LiuShiyuMath at 2026-05-10T02:52:09Z

# Grill — Issue #258: 清理 PR #257 后剩余 3 处 superpowers 残留

按 `docs/HOWTO-PLAN-PR.md` 4 段结构产出。如有歧义先在 `docs/plans/2026-05-10-issue-258/research.md` 记录再请示。

## 1. Plan / 任务描述

PR #257 跑完后，`claudefast -p "in THIS project, what files we have related to superpowers ?"` probe 仍在 V1.5 5-class gate 里漏 1 项 (`scripts/evidence-phase-gaps-ab.ts`)，并暴露了 2 处之前没列入 5-class 的新 stale 残留 (`README.md:282` 用户面文案、root `CLAUDE.md:8-9` dead phase2 doc 路径)。本 PR 把这 3 处全部清掉，让 probe 不再 surface 它们。

**做什么 (what)**

| 文件 | 现状 → 替换为 |
|---|---|
| `scripts/evidence-phase-gaps-ab.ts:219` | `readIfExists(path.join(REPO_ROOT, "docs/superpowers/specs/2026-04-22-product-roadmap-v3.md"))` → `readIfExists(path.join(REPO_ROOT, "docs/backup/phase2-superseded/2026-04-22-product-roadmap-v3.md"))`。原 path 已经是 dead path（文件搬到 `docs/backup/phase2-superseded/`，已 verified by `ls`），脚本里的 `readIfExists` 一直返回空字符串。 |
| `scripts/evidence-phase-gaps-ab.ts:257` | `oldEvidence: ["packages/adapters/src/storage/sqlite/dual-layer-store.ts", "docs/superpowers/specs/2026-04-22-product-roadmap-v3.md"]` → `oldEvidence: ["packages/adapters/src/storage/sqlite/dual-layer-store.ts", "docs/backup/phase2-superseded/2026-04-22-product-roadmap-v3.md"]`。同 above。 |
| `README.md:282` | `teamagent install-plugins    # 装 superpowers / sales / playground 等团队标配 skill` → `teamagent install-plugins    # 装与 .claude/settings.json:enabledPlugins 同步的团队标配插件` |
| `CLAUDE.md:8` | `Phase 2+ 产品 roadmap：docs/superpowers/specs/2026-04-15-product-roadmap.md` → `Phase 2+ 产品 roadmap：docs/superpowers/specs/2026-04-15-product-roadmap-v2.md`。原 path dead（`-v2` 才是 real file，已 verified）。 |
| `CLAUDE.md:9` | `Phase 2 设计：docs/superpowers/specs/2026-04-15-phase2-design.md` → `Phase 2 设计：docs/superpowers/specs/2026-04-15-phase2-design-v2.md`。同 above。 |

**怎么做 (how)**

5 处 surgical edit。无新逻辑、无新测试、无重命名。Boris workflow doc 写在 `docs/plans/2026-05-10-issue-258/{research.md,judge.md}`。

**不做什么 (out of scope)**

- 不删 `scripts/evidence-phase-gaps-ab.ts` 整个文件——它仍被 audit 流程引用，只修 dead path 即可。
- 不重命名 `docs/superpowers/` 目录（合法 milestone 名）。
- 不动其它 `docs/` 文件里的 `docs/superpowers/...` cross-link（probe 允许 surface 它们，因为它们指向 real folder name；只有 dead path 是问题）。
- 不动 `packages/types/src/m5.ts:3` 与 `packages/core/src/validator/l0.ts:44` 的 JSDoc—— 那两个也是 cross-link、合法。
- 不再开 follow-up issue（PR-PLAN）。

## 2. Expected outputs

| 类别 | 路径 | 备注 |
|---|---|---|
| 修改 | `scripts/evidence-phase-gaps-ab.ts` | 2 行 path 替换 |
| 修改 | `README.md` | 1 行替换 |
| 修改 | `CLAUDE.md` (root) | 2 行 doc-path 修正 (`-v2` 后缀) |
| 新文件 | `docs/plans/2026-05-10-issue-258/research.md` | ≤ 80 行 |
| 新文件 | `docs/plans/2026-05-10-issue-258/judge.md` | ≤ 100 行 |

## 3. How-to-verify (judge harness)

V1 RUN — fixed tools:

```bash
WT=/Users/m1/projects/TeamBrain/.codex/worktrees/issue-258
RUN_DIR=$WT/.judge/2026-05-10-issue-258
mkdir -p "$RUN_DIR/raw"; cd "$WT"

# 1. evidence script: no superpowers literal
grep -nE '"docs/superpowers/' scripts/evidence-phase-gaps-ab.ts > "$RUN_DIR/raw/01-evidence.out" 2>&1; echo $? > "$RUN_DIR/raw/01-evidence.exit"
# 2. README L282 user-facing line clean
grep -nE 'superpowers' README.md > "$RUN_DIR/raw/02-readme.out" 2>&1; echo $? > "$RUN_DIR/raw/02-readme.exit"
# 3. root CLAUDE.md doc-paths point to real files
grep -nE 'superpowers' CLAUDE.md > "$RUN_DIR/raw/03-claude.out" 2>&1; echo $? > "$RUN_DIR/raw/03-claude.exit"
ls docs/superpowers/specs/2026-04-15-product-roadmap-v2.md > "$RUN_DIR/raw/03-product-roadmap-v2.exists" 2>&1; echo $? > "$RUN_DIR/raw/03-product-roadmap-v2.exit"
ls docs/superpowers/specs/2026-04-15-phase2-design-v2.md > "$RUN_DIR/raw/03-phase2-design-v2.exists" 2>&1; echo $? > "$RUN_DIR/raw/03-phase2-design-v2.exit"
# 4. typecheck
pnpm typecheck > "$RUN_DIR/raw/04-typecheck.out" 2> "$RUN_DIR/raw/04-typecheck.err"; echo $? > "$RUN_DIR/raw/04-typecheck.exit"
# 5. probe (run from /tmp to avoid project hook attenuation)
( cd /tmp && claudefast -p "I am asking about the project at /Users/m1/projects/TeamBrain. Run: cd /Users/m1/projects/TeamBrain && grep -rln 'superpowers' --include='*.ts' --include='*.md' --include='*.json' . 2>/dev/null | grep -v node_modules | grep -v '.git/' | grep -v '.claude/worktrees/' | grep -v '.codex/worktrees/' | sort. Then return a markdown list of every file path. Skip files inside docs/superpowers/ folder (real milestone folder)." --permission-mode acceptEdits > "$RUN_DIR/raw/05-probe.out" 2> "$RUN_DIR/raw/05-probe.err" ) ; echo $? > "$RUN_DIR/raw/05-probe.exit"
```

V2 DUMP — `judge.json`:

```json
{
  "issue": 258,
  "metrics": {
    "evidence_script_clean": "<bool: step 1 stdout contains 0 literal docs/superpowers/ paths>",
    "readme_clean": "<bool: step 2 stdout has 0 hits>",
    "root_claude_md_links_alive": "<bool: step 3 grep stdout shows -v2 suffix; both -v2 files exist (steps 03-product/phase2)>",
    "typecheck_passed": "<bool: step 4 exit==0>",
    "probe_no_residual_files": "<bool: step 5 stdout does NOT list bin.ts / init.ts / packages/teamagent/CLAUDE.md / m5 fixtures / scripts/evidence-phase-gaps-ab.ts as related-to-plugin-superpowers>"
  },
  "evidence_dir": ".judge/2026-05-10-issue-258/raw/"
}
```

V3 READ — LLM judge:

```
claudefast -p "Read .judge/2026-05-10-issue-258/judge.json + raw/. Return PASS/FAIL. PASS iff all 5 metrics true. For metric 5: FAIL if probe surfaces bin.ts / init.ts / packages/teamagent/CLAUDE.md / m5 test fixtures / scripts/evidence-phase-gaps-ab.ts. OK if probe surfaces docs/superpowers/ folder, docs/CONTEXT.md, docs/FASTPROBE.md, docs/README.md, docs/SELF-UPDATE.md, docs/系统展示/*, docs/HOWTO-PLAN-PR.md, docs/PRODUCT-FEATURES.md, docs/specs/*, docs/features/*, docs/plans/2026-05-10-issue-{253,256,258}/* (these are legit cross-link / historical record)."
```

## 4. claudefast probes (run after merge)

```bash
cd /tmp
claudefast -p "in THIS project, what files we have related to superpowers ?" --permission-mode acceptEdits
```

Expected: probe stdout does NOT mention `bin.ts` / `init.ts` / `packages/teamagent/CLAUDE.md` / m5 fixtures / `scripts/evidence-phase-gaps-ab.ts` as plugin-superpowers-related.

--- end grill ---


---


<!-- end grill log -->
