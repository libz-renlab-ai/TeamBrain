```
   __
 <(o )___       PR #231 iter1 fix-plan
  ( ._> /     /review 在 rebase 后发现 2 处 stale ref
   `---'      （origin/main 的 7 个新 commit 引入的）
```

# PR #231 iter1 fix-plan

适用：worktree-cozy-snacking-otter 分支，rebase onto origin/main 之后。

## Task

PR #231 在 rebase 进 7 个 origin/main 新 commit 之后，又冒出 2 处 mainpi/watcher stale reference。本次 iter1 修复这两处把它们对齐到「禁止 watcher / 后台轮询 / 自动 dispatch」的新政策。

| # | 文件:line | 严重度 | 修法 |
|---|-----------|--------|------|
| 1 | `packages/cli/src/commands/init.ts:1428` | **CRITICAL** (confidence 9/10) | `pnpm teamagent init` 用户可见 CLI banner 写「本地 mainpi 自动: worktree → 实现 → /review fix-loop」+「Step 3-5 全程无人介入」；改写为「maintainer 在 Claude Code 里手动跑 `/fixed-flow-driver` skill」+「无 watcher / 无后台轮询 / 无自动 dispatch」；同步更新「快速验证」probe 从 `5 steps, what's manual vs auto` 改为 `5 steps, who triggers step 3`，与 docs/FIXEDFLOW.md verification 段落保持一致 |
| 2 | `.gitignore:47-48` | INFORMATIONAL (confidence 9/10) | 注释引用已删除的 `scripts/fixed-flow-watcher.sh`；保留 `.fixedflow/` 忽略规则（manual driver 仍写 `.fixedflow/iter-<N>.json` per FIXEDFLOW driver 行为细则），改写注释为「driver runtime state (per-iter token/iter counters)」 |

**Out of scope**（不在本 iter 修，按需开 follow-up issue）：
- `docs/plans/2026-05-09-fixed-flow/judge.md:78,82,170` 仍含 `bash scripts/fixed-flow-watcher.sh --dry-run` 与 `[DRY] would invoke mainpi`——historical audit-trail，按 user-directive 保留不动
- `docs/plans/2026-05-09-pr-200-fix-plan.md:35` 同上
- `docs/plans/2026-05-09-fixed-flow/research.md:96-110` 同上

## Expected outputs

- `packages/cli/src/commands/init.ts:1421-1442` 重写后**不**含字符串 `mainpi`、`无人介入`、`全程`、`auto-dispatch`；**含**字符串 `maintainer`、`手动`、`/fixed-flow-driver`、`无 watcher`
- `.gitignore:47-49` 重写后**不**含字符串 `fixed-flow-watcher.sh`、`watcher runtime state`；**含**字符串 `driver runtime state` 与对 `docs/FIXEDFLOW.md driver 行为细则` 的引用
- `git grep -nE "mainpi|fixed-flow-watcher" -- ':!docs/plans/' ':!docs/archive/'` 结果中**不**应再出现 init.ts 与 .gitignore 命中（只剩 docs/PROJECT-TOOLS.md 与 docs/FIXEDFLOW.md 的显式禁令、cursor-compiler.md 的无关 file-watcher 功能）
- 一个新 atomic commit `fix(fixedflow): pr-231 iter1 — align init.ts banner + .gitignore with manual policy`，挂在 `worktree-cozy-snacking-otter` 分支顶端

## How-to-verify (third-party judge harness)

不让 PR 作者自评。按 docs/PR-PLAN.md 三段铁律，由另一只 LLM 读 raw evidence 当裁判。

### V1 RUN — 固定工具

```bash
# A. 确定性 grep（机器判断）
git grep -nE "mainpi|fixed-flow-watcher" -- \
  ':!docs/plans/' ':!docs/archive/' \
  > .judge/pr-231-iter1/grep.txt 2>&1
echo "exit=$?" >> .judge/pr-231-iter1/grep.txt

# B. CLI 输出语义 probe（LLM-judged）
pnpm teamagent init --dry-run 2>&1 \
  > .judge/pr-231-iter1/init-output.txt
# 或直接读 init.ts 1421-1442 的字符串字面量

# C. claudefast 语义 probe
claudefast -p "如果用户在 fresh 仓库跑 \`pnpm teamagent init\`，输出会告诉他 step 3-5 是谁触发的？" \
  > .judge/pr-231-iter1/init-probe.txt
```

### V2 DUMP — judge.json schema

```json
{
  "pr": 231,
  "iter": 1,
  "evidence_dir": ".judge/pr-231-iter1/",
  "exit_code": 0,
  "metrics": {
    "init_ts_contains_mainpi": false,
    "init_ts_contains_无人介入": false,
    "init_ts_contains_maintainer_manual": true,
    "init_ts_contains_fixed_flow_driver": true,
    "gitignore_contains_watcher_sh": false,
    "gitignore_contains_driver_runtime_state": true,
    "live_surface_grep_zero_results": true
  },
  "stdout_paths": [
    ".judge/pr-231-iter1/grep.txt",
    ".judge/pr-231-iter1/init-output.txt",
    ".judge/pr-231-iter1/init-probe.txt"
  ]
}
```

### V3 READ — LLM-judge

让另一只 LLM 只读 `.judge/pr-231-iter1/judge.json` + `.judge/pr-231-iter1/init-probe.txt`。PASS 条件：

1. 所有 boolean metrics 与上面 expected 值匹配
2. `init-probe.txt` 提到「maintainer」「手动」或「人手」作为 step 3 触发者
3. `init-probe.txt` **不**提到「mainpi」「watcher」「自动 dispatch」「无人介入」作为 live mechanism

任一条 fail → /review 进 iter2，回到 fix-plan 迭代。

## 与既有规则的关系

- `docs/PR-PLAN.md` — 本 fix-plan 即每轮 /review fix 强制三段计划的实例
- `docs/FIXEDFLOW.md` — 本 PR 重写的 canonical spec，本 iter 把 init.ts banner 拉齐到该 spec
- `docs/POSTPR.md` — /review PASS 之后才允许 squash-merge
- ADR-0007 — 本地 `/review` skill 为权威 POSTPR gate
