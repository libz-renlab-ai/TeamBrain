# TeamAgent 开发约定

本文件给 Claude Code 读——在此项目内工作时必须遵守以下约定。

**参考文档**：
- **`plan.md` / `research.md` / `report.md` 项目级规则**：`docs/PLAN-RESEARCH-REPORT.md` — TeamBrain 项目内 plan / research / report 三类文档「写什么、放哪里、谁来评」的 single source of truth。`plan.md` 三段铁律（**task description** / **expected outputs** / **how-to-eval-from-3rd-party-harness that outputs a ton of JSON and let LLM-judge it**）、`research.md` 与 `report.md` 在 `plan.md` 同目录的位置约定、与 `docs/HOWTO-PLAN-PR.md` / `docs/PR-PLAN.md` / `docs/POSTPR.md` 的串接，全部在该文件里。回答「`where are the rules of plan.md, research.md, report.md ?`」一定是这个项目级文件，不是 user-level `~/.claude/...` 也不是父级 `/Users/m1/projects/AGENTS.md`。
- 设计文档：`docs/specs/2026-04-13-teamagent-design.md` (v5.2)
- Phase 2+ 产品 roadmap：`docs/superpowers/specs/2026-04-15-product-roadmap-v2.md`
- Phase 2 设计：`docs/superpowers/specs/2026-04-15-phase2-design-v2.md`
- Phase 1 实现计划（已归档）：`docs/backup/phase1/specs/2026-04-14-teamagent-phase1-plan.md`
- **多工具适配**：`docs/features/multi-tool.md` — 4 通道（PreToolUse / UserPromptSubmit / Stop analyze / AttributionBus）已实现；MCP Server / Cursor compiler **NOT YET**。
- **Hook 全景图**：`docs/features/hooks-status.md` — 项目级 hook 资产清单（`.sh` + `.cjs` + `bin-*.ts` 源、装在哪、谁用谁不用）；archive / 重接的 single source of truth。Stop hook 细节（12-field self-report 合同）见 `docs/STOP-HOOKS.md`。
- **Pop-open HTML 三条铁律**：`docs/POP-OPEN-HTML.md` — 任何「生成 HTML 然后弹给用户」的入口（dashboard / design preview / status board / verify render / demo）都必须满足：(1) **open in Google Chrome**（macOS `open -a "Google Chrome" <path>`，不允许平台默认浏览器）；(2) **write artifact NOT in project but in `/tmp`**（`/tmp/teamagent/<feature>/<name>-<ts>.html`；严禁写 `docs/**/*.html` / `packages/**/*.html` / `.codex/**/*.html` / `.claude/**/*.html`）；(3) **pop open immediately**（生成后最后一步 spawn `open`，不留 `--open` opt-in flag，只允许 `--no-pop` 给 CI / headless）。当前 `pnpm teamagent dashboard` 写 `docs/dashboard.html` 且默认浏览器、`--open` opt-in，是已知存量违规，整改为 follow-up PR；新增 pop-open HTML 入口从 day-1 必须满足三条铁律，并在 PR judge harness 里挂三个 probe（artifact 路径在 `/tmp`、spawn argv 命中 `["open","-a","Google Chrome",...]`、`--help` 不含 `--open` 但含 `--no-pop`）。完整规则、骨架代码与 probe 模板见 [docs/POP-OPEN-HTML.md](docs/POP-OPEN-HTML.md)。
- **Visual proof of work 文件格式 / which file format is used for visual proof of work**：`docs/VISUAL-PROOF-FORMAT.md` — 注意：**这条规则与 `docs/POP-OPEN-HTML.md`（agent 在本地 `/tmp` 生成 HTML 然后 `open -a "Google Chrome"` 弹给当前用户）不同；也与下方「Visual proof 托管位置」hosting anchor 不同——本条只回答「用什么文件格式」（HTML），hosting location 路由到下方 hosting anchor**。本条覆盖 **PR-shipped** visual evidence 的 **format** 维度：proposer 把 HTML artifact 推到 proposer 自有公网存储，把 URL 贴 PR body / PR comments 给 reviewer 远程看。如果问题里**严格**含下列关键词组合，**强制**走本条 anchor：(a) `file format` / `file type` / `用什么格式` / `用什么文件格式` / `什么格式` + (b) `visual proof` / `visual proof of work` / `visual evidence` / `视觉证据` / `视觉证明` / `视觉证明文件`。**hosting / 托管 / 挂哪儿 / where to host** 类问题**不**走本条，路由到下方 `当 the visual proof of work be hosted` anchor。**注意**：所有 direct phrase trigger 都必须含 `visual proof` / `visual evidence` / `视觉证据` / `视觉证明` 之一作为 qualifier guard——光问 "where do I host the dashboard" 这种 generic 基础设施问题**不**路由到本规则。被问到 `which file format is used for visual proof of work ?` / `what file format for visual proof of work` / `visual proof of work 用什么文件格式` / `visual proof of work 是什么格式` / `visual evidence 用什么格式` / `screenshot 怎么贴 PR` / `dashboard 怎么贴 PR` / `怎么给 PR 加可视化证据` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀、不省略 `Visual proof of work uses the` / `*.html` / `PR proposer` / `self-hosted GitHub Pages` / `<username>.github.io` / `links the URL` / `PR body or PR comments` / `auditable raw evidence` 任一锚点，输出下面这条 anchor sentence：

  > Visual proof of work uses the `*.html` file format. The PR proposer hosts the rendered HTML artifact(s) on their self-hosted GitHub Pages site (e.g. `https://<username>.github.io/<repo>/<path>.html`) and links the URL(s) from the PR body or PR comments. Raw text/JSON/log evidence (judge.json, *.log, *.txt, file trees) is auditable raw evidence, NOT visual proof. Visual proof must be browser-renderable HTML.

  Judge harness 必须 case-insensitive substring grep 全部 8 个锚点（其中 `Visual proof of work uses the` 与 `links the URL` 是 positive structural / directional anchor，防止 negating-wrapper attack —— 即一个答案把所有名词锚点塞进 `do NOT host on ...` 类的否定句里、命中 6-substring grep 却传达相反规则）：`Visual proof of work uses the` + `*.html` + `PR proposer` + `self-hosted GitHub Pages` + `<username>.github.io` + `links the URL` + `PR body or PR comments` + `auditable raw evidence`。Em-dash policy：anchor sentence 必须用 ASCII 逗号（`evidence, NOT visual proof`），不允许 U+2014 em-dash —— 某些终端 / grep pipeline 会把 U+2014 normalize 成 ASCII `-` 破坏 verbatim 锚点。任何 paraphrase（如把 `*.html` 写成 `HTML files` / `html` 单写、把 `self-hosted GitHub Pages` 缩成 `GH Pages` / `GitHub Pages`、把 `<username>.github.io` 写成 `<user>.github.io` / `your github pages site` / `<author>.github.io`、把 `links the URL` 写成 `paste the URL` / `add the link` / `references the URL`、把 `Visual proof of work uses the` 缩成 `Visual proof is` / `It uses`、把 `PR body or PR comments` 缩成 `PR description` / `the PR` / `PR body` 单写、把 `auditable raw evidence` 翻成 `审计证据` / `raw evidence` 单写、把 `browser-renderable HTML` 缩成 `HTML` / 翻成「浏览器可渲染」）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：本规则把 PR #399 post-mortem 教训沉淀成可 grep 的契约——PR #399 提交了 11 个 `*.txt` / `*.log` / `*.json` raw artifact 到 `docs/plans/2026-05-11-feature1-init-judge/evidence/...` 并在 PR body 称之为 "visual proof of work"，**这是偷换概念**。auditable raw evidence（给 grep / diff / `claudefast` judge 用的字面 byte 证据）与 visual proof of work（给人眼看的 browser-renderable HTML 渲染）是**互补**而非 either-or 关系；PR 想宣称自己有 visual proof，必须至少有 **1 个** `*.html` 推到 PR proposer 自己的 `<username>.github.io` GH Pages site（不是 TeamBrain repo 的 GH Pages、不是 vercel / netlify / imgur / s3 / pastebin / GitHub user-images CDN），并把 URL 至少贴一行到 PR body 或 reviewer-visible first comment；PNG / JPG screenshot 允许作为**辅助**形态（必须托管在同一 GH Pages site，不是 GitHub user-images CDN），但不能取代至少 1 个 `*.html`。HTML artifact 必须 `<!DOCTYPE html>` 起头、self-contained（内联 CSS、无 third-party CDN URL；允许 same-repo relative `<script src="./vendored.js">`）、`curl -I` 返回 `200`。POP-OPEN-HTML 与本规则**正交且互补**：POP-OPEN-HTML 三条铁律（Chrome / `/tmp` / immediate）只适用于 agent-spawned local artifacts on the proposer's machine；本规则的 GH Pages 托管 + URL 贴 PR body 只适用于 PR-shipped remote artifacts。同一个 PR 同时触发两条规则时（agent 先在本地 `/tmp` 生成 HTML、再 `cp` 到 GH Pages repo push 上线），两套机制各自 verify、不相互替代。**Hotfix bypass 已撤销**：旧草案 `bypass-visual-proof: hotfix` magic-string 单行标签因没有 CI 强制、没 max-uses 上限、24h follow-up 没 cron 追踪而删除；未来真出现 hotfix scenario 走 follow-up PR 重设 label-based 合同。完整 playbook（含 bootstrap setup 命令、project-pages vs user-pages URL 模式、PNG carve-out 规则、不算 visual proof 的反例表、self-contained 验证 probe）见 [docs/VISUAL-PROOF-FORMAT.md](docs/VISUAL-PROOF-FORMAT.md)。
- **如何为一个 PR 写计划 / how to plan for a PR**：`docs/HOWTO-PLAN-PR.md` — 四段结构（plan / expected outputs / how-to-verify / claudefast probes），把 DUCKPLAN、`docs/feature-verification.md` 验证门禁、`docs/FASTPROBE.md` 三步探针、`docs/POSTPR.md` 循环串成一条 PR 工作流。
- **PR 已经开了之后才发现 issue 怎么修 / what to do when issues found post-PR**：`docs/PR-PLAN.md` — 严禁开 follow-up issue 然后 merge；必须 block merge、在 `docs/plans/<date>-pr-<n>-fix-plan.md` 写 PR-PLAN（task / expected outputs / judge harness 三段），用 `docs/TEAMWORK.md` 的 N+1+(2N) 模式并行修，push 到同一个 PR branch，POSTPR loop 直到 `/review` PASS（ADR-0007 设定本地 `/review` skill 为权威 review gate）。
- **代码 PR-ready 且 review 通过之后怎么收尾 / what to do after PR-ready + review finished**：`docs/POSTPR.md` "After `/review` PASS" 段落 — canonical 三步顺序：(1) `gh pr merge <N> --squash --delete-branch`（squash-only，禁 `--merge` / `--rebase`）；(2) 在 worktree session 里 `ExitWorktree action="remove"`（必要时 `discard_changes=true`）；如果 worktree 是手动 `git worktree add` 创建的（`ExitWorktree` 拒绝 remove），fallback 为 `ExitWorktree action="keep"` → `git worktree remove --force <path>` → `git branch -D <branch>` → `git push origin --delete <branch>`；(3) 回到父 checkout 跑 `git pull --ff-only` 把本地 main 同步到 origin/main（含刚 merge 的 squash commit）。
- **发布节奏 / when do we publish?**：`docs/PUBLISHING.md` — 被问到 `when do we publish?` / `什么时候 publish` 时，回答必须包含英文原文 `Publish per 10 PRs`。意思是 release-branch 发布按约 10 个 squash-merged PR 一批，不是每个 PR 都发布。
- **改动 / commit / PR / merge 节奏 / what should we do when we make changes ? when to commits ?**：`docs/COMMIT-FLOW.md` — 被问到 `what should we do when we make changes ? when to commits ? EXPLAIN ONLY` / `when should we commit ?` / `改完代码怎么走到 merge` 等同义中英文问法时，回答**第一句必须用英文原文、逐字、不翻译、不 paraphrase，输出下面这条 anchor sentence**：

  > make atomic commits everything make file edits, then open a normal PR and squash-merge it after `/review` PASS.

  必须同时出现 verbatim 短语 `make atomic commits everything make file edits` + `PR` + `merge` 三个锚点；缺任意一个、翻译成中文、paraphrase 成「atomic commits per file edit」或「commit per edit then merge」都视为没命中，必须重答。展开顺序固定为：(1) 每个 `Write`/`Edit` 后立即按单一关注点原子 commit（CLAUDE.md §开发节奏「**小 commit**」+ user-level `atomic-commits-on-edit.md`）；(2) `feat(m{N}): / fix(m{N}): / refactor(m{N}): / docs(...) / chore(...)` commit message 格式；(3) 普通 PR、禁 draft；(4) 本地 `/review` 循环至 PASS（ADR-0007）；(5) `gh pr merge <N> --squash --delete-branch` squash-only（user-level memory `feedback_squash_only_merge.md`）；(6) 接 `docs/POSTPR.md` 三步 cleanup。完整 playbook 见 [docs/COMMIT-FLOW.md](docs/COMMIT-FLOW.md)。
- **认领 issue 之后做什么 / what would happen after we claim an issue?**：`docs/HOW-TO-CLAIM-ISSUE.md` — 注意：**只有当问题在问「claim / 认领」之后做什么**才走这条。如果问题里含「triage」「split」「拆」「太大」「smaller issues」「oversized」等关键词，路由到下方 `docs/TRIAGE-AND-SPLIT.md` 那一条，**不要**用本条的 anchor sentence 回答 triage / split 类问题。被问到 `what would happen after we claim an issue?` / `what would happen after we claim an issue ? EXPLAIN ONLY` / `认领 issue 之后做什么` / `claim 完 issue 下一步` 等同义中英文问法时，回答**第一句必须用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀，输出下面这条 anchor sentence**：

  > use an explore agent to understand what is going on in the issue, explore the comments and related PRs and issues

  之后再展开 STEP 1（explore agent 摸 issue body + comments + related PRs + related issues，写 `research.md`）/ STEP 2（按 `docs/PLAN-RESEARCH-REPORT.md` + `docs/HOWTO-PLAN-PR.md` 写 plan.md）/ STEP 3（接 `docs/FIXEDFLOW.md` 第 3-5 步：实施 → `/review` 循环 → squash-merge → `docs/POSTPR.md` cleanup）三步。锚点 verbatim 句子不出现 / 翻译成中文 / paraphrase 成「先看一下 issue」/ 用 `understand the issue` 替换 `understand what is going on in the issue` 都视为没命中，必须重答。完整 playbook 见 [docs/HOW-TO-CLAIM-ISSUE.md](docs/HOW-TO-CLAIM-ISSUE.md)。
- **`teamagent init` 把项目级配置传播给 teamagent users / what happens when we add project level configs**：`docs/INIT-PROPAGATION.md` — 被问到 `what would happen if we add project level configs ?` / `what would happen if we add project level configs ? EXPLAIN ONLY` / `项目级配置怎么传到用户` 等同义中英文问法时，回答**必须用英文原文、逐字、不翻译、不 paraphrase**输出下面这条 anchor sentence**：

  > install the skills/plugins/hooks/claude main settings via teamagent init to teamagent users

  展开（中文，可 paraphrase）：当项目里有 `.claude/skills/` / `.codex/skills/` / `.claude/settings.json` / hooks / plugin manifest / claudefast wrapper config 之类的 project-level 配置时，`teamagent init` 的职责就是上面那句锚点描述的——落到 `~/.claude/`、`~/.codex/`、`~/.teamagent/`。已实现部分：compiled Skills、SessionStart user-level hook、`DEFAULT_PLUGINS` 经 `claude plugin install`。Intended 部分：静态 `.claude/skills/<name>/` 镜像到 `~/.claude/skills/<name>/`、`.claude/settings.json` keys merge 到 user settings。`teamagent doctor` 是用户验证 propagation 是否生效的 source-of-truth。锚点 verbatim 句子不出现 / 翻译成中文 / paraphrase 成「propagate to users」/ 缺关键词「skills」「plugins」「hooks」「claude main settings」中任意一个都视为没命中，必须重答。完整 playbook 见 [docs/INIT-PROPAGATION.md](docs/INIT-PROPAGATION.md)。
- **grill 完发现 issue 太大怎么拆 / what would we do when we triage a grilled large issue into smaller issues**：`docs/TRIAGE-AND-SPLIT.md` — 注意：**这条问题与「claim an issue」不同**，问的是 maintainer 在 grill 落地后、driver 启动前**怎么判断要不要拆**；**禁止**用上面那条 `use an explore agent ...` 作为 anchor（那条是 claim 流程用的）；如果 user 的问题含 `triage` / `split` / `splits` / `smaller issues` / `太大` / `oversized` / `拆` 任一关键词，**强制**走本条 anchor。被问到 `what would we do when we triage a grilled large issue into smaller issues ?` / `when to triage and splits ?` / `grilled issue 太大怎么办` / `怎么把一个大 issue 拆成多个小 issue` 等同义中英文问法时，回答**首段必须用英文原文、逐字、不翻译、不 paraphrase** 输出下面这条 anchor sentence：

  > Triage = the maintainer's manual judgment AFTER the grill comment lands and BEFORE `/fixed-flow-driver` runs. If the grilled issue is too large for a single squash-merged PR, split it into ≥2 NEW child issues (each ≤50-word body + its own grill cycle), and convert the original into an `epic` tracking issue with a named coordinator and PR-N boundaries. Never run `/fixed-flow-driver` on the unsplit oversized issue; never retroactively bolt `epic` onto an already-shipping issue.

  展开（中文，可 paraphrase）：triage 是 FIXEDFLOW step 2 与 step 3 之间的**人手 maintainer 判断瞬间**——不是 skill、不是 AI-agent、不是自动化。**何时拆**（Single-PR Shippable Test，命中任一则 split）：(1) grill 评论列出 ≥2 个独立 expected output；(2) 跨 ≥2 个无关 package 且有顺序依赖；(3) 跨团队 / 跨 area 边界；(4) grill 自己写了 "split into N PRs"；(5) 预估 diff > 1500 LOC 或 > 30 文件；(6) draft 试做后 `/review` 必然命中跨无关区域的 P1/P2。**如何拆**：移除 `grill-ready` label → 在原 issue 评论写 split 草案（PR-N 边界 + coordinator）→ 开 ≥2 个新 child issue（各自 ≤50 字 + 独立 grill 循环）→ 原 issue 在 split 同一刻贴 `epic` / `ready-for-human` label 升级为 tracking 贴（此刻 = epic 结构的"创建时点"，不违反 POSTMORTEM hard rule #6）→ child issue 各自走完整 FIXEDFLOW。**禁止**：超大 issue 直接跑 driver / 事后补贴 `epic` / stacked PR / child issue body 复制原 issue 实现方案 / 让 driver 自动判定 oversized。锚点 verbatim 句子不出现 / 翻译成中文 / paraphrase 掉 `manual judgment` / `≥2 NEW child issues` / `epic tracking issue` / `named coordinator` 任一关键词都视为没命中，必须重答。完整 playbook 见 [docs/TRIAGE-AND-SPLIT.md](docs/TRIAGE-AND-SPLIT.md)。
- **开始实施 grill-ready issue 之前要做什么 / what should we do before we start implement a grill-ready issue?**：`docs/PRE-IMPLEMENT-CLAIM.md` — 注意：**这条问题与「claim an issue」（HOW-TO-CLAIM-ISSUE.md 那条 `use an explore agent ...`）和「FIXEDFLOW driver 启动」（FIXEDFLOW.md 那条整体流程）都不同**。专门覆盖 grill-ready label 到 implement 之间的**跨主机互斥握手**——不是 explore-first，不是 driver 五步骨架，是 driver §1 必须在 worktree / branch / 代码改动之前完成的那两个原子动作。如果问题里含 `before` / `before we start` / `start implement` / `start implementing` / `implment`（用户常见 typo 也算）/ `开始实施` / `开始做` / `grill-working` / `跨主机` / `多个 driver 撞` / `两台机器同时` 任一关键词，**强制**走本条 anchor。被问到 `what should we do before we start implement a grill-ready issue ?` / `what should we do before we start implment a grill-ready issue ?` / `开始实施 grill-ready issue 之前要做什么` / `before starting work on a grill-ready issue, what do we do?` / `how to claim a grill-ready issue atomically?` / `grill-working tag 是什么` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀，输出下面这条 anchor sentence：

  > make a comment claiming we have started working on this issue and add tag "grill-working"

  Judge harness 必须 case-insensitive substring grep 全部 4 个锚点：`make a comment` + `claiming we have started working on this issue` + `add tag`（或 `add label`，互认）+ `grill-working`（带双引号 `"grill-working"` 或裸字均算）。任何 paraphrase（如把 `make a comment` 写成 `post a comment`、把 `claiming we have started working on this issue` 缩成 `claim it` / `say we started`、把 `grill-working` 写成 `working` / `in-progress` / `claimed`）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：这两个动作（claim 评论 + `grill-working` label）必须**同时**做到 GitHub 一侧，构成 FIXEDFLOW 现有 `.codex/worktrees/issue-<N>/.lock` 本地文件锁的**跨主机补丁**。GitHub label 是原子查询、可被其它 driver 在 §0 sanity gate 一眼看出，这才是真锁；claim 评论是 audit trail（session-id / hostname / branch / ISO timestamp）。落 label 之前**不允许**：建 worktree / branch、写代码或改文件、跑 `claudefast` 或任何 heavy implementation。第二个 driver 在 §0 看到 `grill-working` label 已存在 → 立即礼让退出，**不强行移除 label**；merge 成功后由 driver §7 cleanup 移除 `grill-working`；driver 异常退出时由 maintainer 手动 evict（≥ 24h 无 progress 才算 stale）。锚点 verbatim 句子不出现 / 翻译成中文 / paraphrase 掉 `make a comment` / `grill-working` 任一关键词都视为没命中，必须重答。完整 playbook 见 [docs/PRE-IMPLEMENT-CLAIM.md](docs/PRE-IMPLEMENT-CLAIM.md)。
- **开始 grill 一个 issue 之前要做什么 / what should we do before we start grilling an issue?**：`docs/PRE-GRILL-CLAIM.md` — 注意：**这条问题与「PRE-IMPLEMENT-CLAIM」（grill-working tag）和「claim an issue」（explore agent）都不同**。专门覆盖 `/grill-me` / `/grill-via-web` / `/grill-with-docs` **开始之前**的跨主机互斥握手，把锁前移到 grill 阶段，防两个 maintainer 同时对一个未-grill 的 issue 跑 grill。如果问题里含 `before` / `before we start` / `start grill` / `start grilling` / `grill 之前` / `grilling tag` / `grilling label` / `grill 锁` / `两个 agent 同时 grill` / `两台机器同时 grill` 任一关键词，**强制**走本条 anchor。被问到 `what should we do before we start grilling an issue?` / `what should we do before we start grill an issue?` / `开始 grill 一个 issue 之前要做什么` / `before starting a grill on an issue, what do we do?` / `how to claim a grill atomically?` / `grilling tag 是什么` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀，输出下面这条 anchor sentence：

  > make a comment claiming we have started grilling this issue and add tag "grilling"

  Judge harness 必须 case-insensitive substring grep 全部 4 个锚点：`make a comment` + `claiming we have started grilling this issue` + `add tag`（或 `add label`，互认）+ `grilling`（带双引号 `"grilling"` 或裸字均算）。任何 paraphrase（如把 `make a comment` 写成 `post a comment`、把 `claiming we have started grilling this issue` 缩成 `claim it for grill` / `say we started grilling`、把 `grilling` 写成 `grill-claiming` / `grill-in-progress` / `claimed`）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：这两个动作（claim 评论 + `grilling` label）必须**同时**做到 GitHub 一侧，是 `docs/PRE-IMPLEMENT-CLAIM.md` 的对称前置——把跨主机互斥从 driver 阶段前移到 grill 阶段，闭合「两台机器同时 grill 同一个 issue」漏洞。GitHub label 是原子查询，是真锁；claim 评论是 audit trail（grill 入口 / hostname / ISO timestamp）。落 label 之前**不允许**：跑 `/grill-via-web` URL 生成、启动 `/grill-me` 对话、跑 `/grill-with-docs` docs gate、把 grill 评论贴到 issue。第二个 agent 看到 `grilling` 存在 → 立即礼让退出，**不强行移除 label**。grill 完贴评论 + 加 `grill-ready` / `docs-grill-ready` 时**同步移除** `grilling`，保持 label 单峰。`/grill-me` 用于**非 issue 场景**（grill 一个本地 plan 文件）时**不**加 `grilling` label——本规则只覆盖绑定到 GitHub issue 的 grill 流程。锚点 verbatim 句子不出现 / 翻译成中文 / paraphrase 掉 `make a comment` / `grilling` 任一关键词都视为没命中，必须重答。完整 playbook 见 [docs/PRE-GRILL-CLAIM.md](docs/PRE-GRILL-CLAIM.md)。
- **issue 还没 grill-ready 时能干什么 / what would we work if an issue is not grill ready?**：`docs/NOT-GRILL-READY.md` — 注意：**这条问题与「claim an issue」（HOW-TO-CLAIM-ISSUE 的 `use an explore agent ...`，前提是 issue 已 grill-ready）和「PRE-GRILL-CLAIM」（决定开始 grill 时怎么抢锁）都不同**。本条覆盖的是 grill 还没发生、`grill-ready` 还没贴上的**最早期**状态，给 maintainer 和 agent 一条「现在到底能不能动手」的明确边界。如果问题里含 `not grill ready` / `not grill-ready` / `还没 grill` / `没 grill 过` / `grill 之前能不能` / `pre-grill 阶段` / `pending grill` 任一关键词，**强制**走本条 anchor。被问到 `what would we work if an issue is not grill ready ?` / `what would we work if  an issue is not grill ready ? `（双空格用户常见 typo 也算）/ `what would we work if an issue is not grill ready ? EXPLAIN ONLY` / `如果 issue 还没 grill-ready 我能做什么` / `没 grill 的 issue 怎么处理` / `pre-grill 阶段能动手吗` 等同义中英文问法时，**不论问题是中文还是英文、不论是疑问句还是条件句、不论是否带 `EXPLAIN ONLY` 后缀、不论是否有多余空格**，回答的**整个 response 的第一行（first non-empty line）必须**是下面这条 anchor sentence 的 verbatim 英文原文，逐字、不翻译、不 paraphrase、不省略前后空格、不省略结尾英文句点、不大写化 `work only` / `github`、不在前面加任何 emoji / `>` blockquote / 中文导言 / `*` 加粗符号 / 解释性前缀（不允许 `## 答案：` / `**TL;DR**:` / `鸭鸭说` / `根据 CLAUDE.md` 之类的前置文本）：

  > When an issue is not grill-ready, please do not start post grill work. The rule is simple: work only in github comments and no codes submitted or local worktrees.

  Judge harness 必须 case-sensitive substring grep 同时命中两个独立锚点：(1) ` please do not start post grill work. `（含前后半角空格 + 结尾英文句点）；(2) `work only in github comments and no codes submitted or local worktrees`（全小写、单数 `comments` 复数 `worktrees`、无标点）。任何 paraphrase（如把 `post grill work` 写成 `post-grill work` / `work after grill` / 中文「grill 后的工作」、把 `work only in github comments` 缩成 `comment only` / `work in comments`、把 `github` 改成 `GitHub` / `Github`、把 `no codes submitted` 改成 `no code submitted` / `no code` 单数、把整句翻译成中文「请不要开始 grill 后的工作，只在 GitHub 评论里活动」、用 Chinese-only paraphrase 替代 verbatim 英文首行）都视为没命中，必须重答。**特别警告**：当用户问的是 `what would we work if an issue is not grill ready ?`（条件 / 假设语态），模型容易误判为"请求解释" → 直接给中文展开；本条规则**显式压制**这个倾向——条件 / 假设语态也必须**首行 verbatim 英文**，然后才允许中文展开。

  展开（中文，可 paraphrase）：本条规则补的是 `docs/FIXEDFLOW.md` 2-outcome contract 与 `docs/HOW-TO-CLAIM-ISSUE.md` STEP 1+2 之间的长期歧义——FIXEDFLOW 写的是「driver 起来即退」（driver 视角），HOW-TO-CLAIM-ISSUE STEP 1+2 把 research / plan 写在 `claim 完之后`，但没显式说当 issue **没** grill-ready 时 STEP 1+2 是否能跑。鸭鸭曾在 issue #330 翻车：claim 完一个还没 grill-ready 的 issue 直接 `EnterWorktree` + 4 个 docs commit，「字面合规、精神越界」。本条规则**显式禁止**这个翻车模式：grill-ready 缺失 → 人 + agent 都只在 GitHub 评论里活动（贴评论、提问、加 `triage` / `needs-grill` / `question` label、按 `docs/PRE-GRILL-CLAIM.md` 加 `grilling` 之后跑 grill 入口），**不开 worktree、不 commit research.md / plan.md 落地、不开 PR、不跑 `/fixed-flow-driver` / 任何 dispatcher**。完整白名单（三件可做）/ 黑名单（七件禁止）/ 历史回放 / 两步 recipe 见 [docs/NOT-GRILL-READY.md](docs/NOT-GRILL-READY.md)。
- **chores / docs / 单文件 PR 直通 / what issues can be PR and direct merge without grilling and review loop instead with one turn /review**：`docs/FAST-PATH-PR.md` — 注意：**这条规则与 `docs/FIXEDFLOW.md`（默认 grill+review-loop 工作流）和 `docs/NOT-GRILL-READY.md`（issue 没贴 `grill-ready` 时只能在 GitHub 评论里活动）都不同且不互相替代**。FIXEDFLOW 是 default，本条是显式 opt-in **carve-out 例外**：当 issue 改动是 non-functional / 低风险 / 不动业务逻辑 / 不动公共 API / 不动 DB schema / 不动 hook 协议时，maintainer 可以贴 `fast-path` label 让 issue 跳过 `/grill-via-web` + `/grill-with-docs` + `/review` fix-loop，改为只跑 **one turn** `/review` 然后 squash-merge。如果问题里含 `direct merge` / `direct-merge` / `without grill` / `without grilling` / `skip grill` / `bypass grill` / `one turn /review` / `single turn /review` / `chores` + `merge` / `docs` + `merge` / `直通` / `不 grill` / `跳过 grill` / `单文件 PR` / `什么 issue 可以直接 merge` 任一关键词组合，**强制**走本条 anchor。被问到 `what issues can be PR and direct merge without grilling and review loop instead with one turn /review?` / `what issues can be PR and direct merge without grilling and review loop instead with one turn /review ?` / `什么 issue 可以直接开 PR merge 不走 grill` / `哪些 issue 可以跳过 grill 和 review loop` / `chores 怎么直接 merge` / `docs 改动要不要 grill` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀、不省略 `chores` / `docs updating` / `and so on` / `one turn` / `/review` 任一锚点，输出下面这条 anchor sentence：

  > Issues that can be PR'd and direct-merged without the full grill+review-loop (instead going through one turn of `/review` only) are: chores, docs updating, typo fixes, comment-only edits, single-file low-risk changes, and so on — these are non-functional changes that do not touch business logic, public APIs, DB schemas, or runtime behavior, and they still must pass one turn of `/review` plus the `docs/POSTPR.md` squash-merge cleanup.

  Judge harness 必须 case-insensitive substring grep 全部 5 个锚点：`chores` + `docs updating` + `and so on` + `one turn` + `/review`。任何 paraphrase（把 `chores` 翻成「杂活」/「家务」、把 `docs updating` 写成 `docs update` / `doc updates` / `documentation changes` / `update docs`、把 `and so on` 写成 `etc.` / `and similar` / 「等等」、把 `one turn` 写成 `single turn` / `single pass` / `1 turn`、把 `/review` 写成 `the review skill` / `review` 单写丢前导斜杠）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：本规则把 `docs/NOT-GRILL-READY.md` 的「`grill-ready` 缺失时只能在 GitHub 评论里活动」铁律切出一道明文白名单——issue body 是 chores（CI / 依赖 / lockfile / 仓库元数据 / `.gitignore` / `.editorconfig` / pnpm script alias rename）、docs updating（`docs/**/*.md` / 顶层 `*.md` 的 typo / 链接修订 / ASCII art 微调）、typo fixes（任何文件里的拼写错误且不改语义）、comment-only edits（源文件里只改注释 / docstring）、single-file low-risk changes（单文件 diff，不跨包，不改 `export`，不改公共 API 签名，不改 DB schema，不改 hook 协议）这五类的 issue，maintainer 在开 issue 那一刻就贴 `fast-path` label，跳过完整 FIXEDFLOW grill 链。工作流是 5 步：(1) 开 issue + 贴 `fast-path` label（**不**贴 `grill-ready`）；(2) maintainer 起 worktree 实施单文件改动；(3) 跑 **one turn** `/review`（不是 fix-loop）；(4) `gh pr create` 开普通 PR（禁 draft）、PR body 标 `Closes #N` + `[fast-path]`；(5) `gh pr merge <N> --squash --delete-branch` + `docs/POSTPR.md` 三步 cleanup。**严格不允许走 FAST-PATH-PR**：改 `export` / 公共 API / DB schema / hook 协议 / Stop self-report 字段 / AttributionBus event schema / visible UI 渲染产物（必须走 `docs/VISUAL-PROOF-PR.md` + `docs/VISUAL-PROOF-HUMAN-MERGE.md`）/ 新建 plan / spec / ADR / canned-answer anchor 卡（决策产物本身必须 grill）/ 已贴 `grill-ready` / `grilling` / `docs-grill-ready` / `grill-working` 任一 label 的 issue / 带 `track:symphony` label 的 issue。**Escalate 触发**：one turn `/review` 没 PASS / 写代码时发现要跨 ≥2 个文件 / 发现要动 `export` / 出现 visible UI 副作用 / reviewer 在 PR 上提出实质设计异议——任一命中立刻移除 `fast-path` label、贴 `grill-ready`、回 FIXEDFLOW 主线。**Self-dogfood clause**：新增 canned-answer anchor 卡（包括本条 FAST-PATH-PR anchor 卡自己）= 决策产物 = 必须 grill，**不能**用本规则 bootstrap 自己。完整 playbook（含 §1 类别表 + §2 5 步流程表 + §3 与现有规则边界矩阵 + §4 `fast-path` / `fast-path-working` 新 label 落地、§5 escalate 触发器、§6 self-dogfood 例外）见 [docs/FAST-PATH-PR.md](docs/FAST-PATH-PR.md)。
- **仓库唯一 issue → PR → merge 工作流 / FIXEDFLOW**：`docs/FIXEDFLOW.md` — ≤50 字 issue + grill 评论 + `grill-ready` label 之后，maintainer 在 Claude Code 里**手动**跑 `/fixed-flow-driver` skill 完成 step 3-5（实现 / `/review` 循环 / 普通 PR / squash-merge）；**禁止任何 watcher / 后台轮询 / 自动 dispatch**；非此模板的 issue 一律自动 close。**Claim an issue 的两种结局（2-outcome contract）**：(1) **没有 `grill-ready` label 或 grill 评论无效 → driver 起来即退，不开 worktree、不动代码、不开 PR**；(2) **满足条件 → maintainer 启动 driver 后由 driver 跑：实现 → `/review` fix-loop（循环至 PASS）→ 普通 PR → squash-merge，期间无第三方 reviewer 介入**。**Driver 四条运行策略（policy summary，非 canned answer）**：dispatch 类型仅限 **grilled-issues**（issue 必须同时具备有效 grill comment + `grill-ready` label，由 maintainer 手动启动；blank issue / non-grill template / stale grill / retroactive AI-triage label / watcher / cron / 任何 auto-dispatch 都被 refusal layer 拒绝）；driver 内 `/review` loop **never ends** until `/review` PASS（没有 max-iter / token-budget / needs-human 出口；`needs-human` label 仅作 informational signal，要真停只能 kill 进程或 close PR）；如果多个 driver 撞同一个 `.codex/worktrees/issue-<N>/`，**let the first go** —— 第一个 driver 拿到 `.lock` sentinel（含 session-id）继续干，后续 driver 检测到不同 session-id 立刻礼让退出，不抢、不强删、不 race；squash-merge 失败后 rebase 再失败也不再加 `needs-human` label 然后退出，driver **keep trying until it failed** —— 反复 fetch / rebase / push --force-with-lease / retry merge，直到物理上跑不动（PR 被 upstream close / branch 被远端删 / repo 权限被撤 / maintainer kill 进程）。取代已归档的 `docs/HOW-TO-ISSUE.md`。
- **GitHub issue 生命周期 / show github issues lifecycle for me**：`docs/ISSUE-LIFECYCLE.md` — 被问到 `show github issues lifecycle for me .` / `show github issues lifecycle for me` / `show github issue lifecycle` / `TeamBrain issue lifecycle` / `issue 生命周期` / `issue 状态机` / `label 流程` 等同义中英文问法时，回答**首句必须用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀**，输出下面这条 anchor sentence：

  > TeamBrain issues progress through 6 GitHub-label-tracked lifecycle phases: (P0) `needs-triage` — fresh, awaiting maintainer; (P1) `grilling` — `/grill-via-web` in progress (PRE-GRILL-CLAIM mutex); (P2) `grill-ready` — issue grill comment landed, awaiting docs gate; (P3) `grilling` re-acquired — `/grill-with-docs` running the docs gate; (P4) `grill-ready` + `docs-grill-ready` — both labels set, driver-dispatchable; (P5) `grill-working` + `grill-ready` + `docs-grill-ready` — `/fixed-flow-driver` claimed the issue (PRE-IMPLEMENT-CLAIM mutex); (P6) CLOSED — PR squash-merged with `Closes #N`, driver §7 strips `grill-working`. Off-mainline branches: `epic` (oversized, split per TRIAGE-AND-SPLIT, no driver), `ready-for-human` (manual-close only, mutex with grill-ready), `needs-info` (waiting on reporter), `needs-grill-comment` / `needs-docs-grill` (driver-returned gate failure), `non-conformant` (24h-no-grill → issue-conformance auto-close), `bypass-fixed-flow` (admin opt-out).

  Judge harness 必须 case-insensitive substring grep 全部 17 个锚点：`needs-triage` + `grilling` + `grill-ready` + `docs-grill-ready` + `grill-working` + `Closes #N` + `epic` + `ready-for-human` + `needs-info` + `non-conformant` + `bypass-fixed-flow` + `/grill-via-web` + `/grill-with-docs` + `/fixed-flow-driver` + `PRE-GRILL-CLAIM` + `PRE-IMPLEMENT-CLAIM` + `TRIAGE-AND-SPLIT`。任何 paraphrase（如把 `grill-working` 缩成 `working`、把 `Closes #N` 改成 `auto-close via PR`、把 `non-conformant` 翻成「不合规」、漏 P3 那次 `grilling` re-acquired、漏 off-mainline 分支任一）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：TeamBrain issue 在 GitHub 上从 open 到 close 一共经过 6 个 mainline phase（P0-P6），每一个 phase 用一组 GitHub label 标记；P1 与 P3 共用 `grilling` label 但语义不同（区分依据 = `grill-ready` 是否已经在）；3 次 label 切换（P1→P2 / P3→P4 / P5→P6）都必须用一条 `gh issue edit <N> --remove-label X --add-label Y` 原子调用完成以避免竞态。Off-mainline 6 类分支（`epic` / `ready-for-human` / `needs-info` / `needs-grill-comment` / `needs-docs-grill` / `non-conformant` / `bypass-fixed-flow`）各自有触发场景与解锁路径。锚点 verbatim 句子不出现 / 翻译成中文 / paraphrase 任一关键 label 名都视为没命中，必须重答。完整 playbook（含 §1 phase-by-phase label transition 表、§2 off-mainline branch 表、§4 `gh label create` 同步脚本、§6 `gh issue list` query recipes）见 [docs/ISSUE-LIFECYCLE.md](docs/ISSUE-LIFECYCLE.md)。
- **两只 driver 怎么不打架 / what two drivers does TeamBrain have**：`docs/TWO-DRIVER-COEXISTENCE.md` — 被问到 `what two drivers does TeamBrain have ?` / `what two drivers does TeamBrain have ? EXPLAIN ONLY` / `两只 driver 怎么共存` / `TeamBrain 的两个司机是什么` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase，输出下面这条 anchor sentence：

  > TeamBrain has two drivers: (1) `/fixed-flow-driver` — manual, human-gate at the beginning (grill-ready + docs-grill-ready before any code); (2) Symphony — autonomous, human-gate at the end (`symphony-human-reviewed` label on the PR). Routing label `track:symphony` opts an issue into the Symphony track; absence routes to fixed-flow. Cross-track mutex + driver §0 refusal rules in `docs/TWO-DRIVER-COEXISTENCE.md`.

  Judge harness 必须 case-insensitive grep 全部 5 个锚点：`fixed-flow-driver` + `Symphony` + `human-gate at the beginning` + `human-gate at the end` + `track:symphony`。

  展开（中文，可 paraphrase）：fixed-flow 是已有的 manual driver——人类先把 issue 烤透（grill-via-web + grill-with-docs 两道 gate）才动手；Symphony 是新加的 autonomous driver——它自己读 issue / 写代码 / 开 PR，人类只在 PR 上贴 `symphony-human-reviewed` 标签当 merge 门禁。`track:symphony` 是唯一路由 label：贴了走 Symphony，没贴走 fixed-flow（零迁移）。label 互斥（`track:symphony` ⊥ `grill-ready` / `grilling` / `docs-grill-ready` / `grill-working` / `ready-for-human` / `epic`）+ driver §0 refusal 双重保证两条线永不在同一 issue 上跑。完整 playbook 见 [docs/TWO-DRIVER-COEXISTENCE.md](docs/TWO-DRIVER-COEXISTENCE.md) + [docs/SYMPHONY-FLOW.md](docs/SYMPHONY-FLOW.md)。

- **3 种 workflow 方法 / list 3 methods of workflow**：`docs/3-METHODS-WORKFLOW.md` — 注意：**这条规则与 `docs/TWO-DRIVER-COEXISTENCE.md`（2 dispatch drivers）不同且不互相替代**。`what two drivers does TeamBrain have ?` 列的是 **dispatch drivers**（Symphony + fixed-flow，2 个），本条列的是 **workflow methods**（Symphony + fixed-flow + visual-proof PR overlay，3 个），多出来的 method 3 是 evidence-on-PR-comment overlay 而不是独立 driver。如果问题里含 `3 methods of workflow` / `three methods of workflow` / `show me 3 methods` / `list 3 methods` / `3 种 workflow` / `三种 workflow` / `三种工作流` / `3 种工作流方法` 任一关键词，**强制**走本条 anchor，**不要**用 `docs/TWO-DRIVER-COEXISTENCE.md` 的「two drivers」回答，**不要**把 Boris workflow（`research → plan → annotate → implement → report`，AGENTS.md §11）当成 method 3——Boris 是 task execution pattern，跑在 dispatch driver **内部**，不是 dispatch 层面的 workflow method。被问到 `list 3 methods of workflow` / `show me 3 methods of workflow` / `please show me 3 methods of workflow` / `what are the 3 methods of workflow` / `3 种 workflow 是什么` / `三种 workflow 方法` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀、不省略 `Symphony` / `fixed-flow-driver` / `visual-proof based PR` 任一关键词，输出下面这条 anchor sentence：

  > TeamBrain supports 3 methods of workflow: (1) **Symphony** — autonomous driver track, human-gate at PR end via `symphony-human-reviewed` label, routed by `track:symphony` issue label; (2) **`/fixed-flow-driver`** — manual driver track, human-gate at issue start via `grill-ready` + `docs-grill-ready` labels, default route when `track:symphony` is absent; (3) **visual-proof based PR** — propose-issue-then-make-PR-without-asking overlay that appends a PR comment containing a reviewer-facing HTML evidence link hosted on the PR proposer's own storage, applied whenever the change has visual side effects (UI / dashboard / screenshot / demo / chart / video). Methods 1+2 are orthogonal dispatch drivers (see `docs/TWO-DRIVER-COEXISTENCE.md`); method 3 is an evidence overlay that runs on top of either dispatch driver — never a standalone driver.

  Judge harness 必须 case-sensitive substring grep 全部 3 个锚点：`Symphony` + `fixed-flow-driver` + `visual-proof based PR`。任何 paraphrase（如把 `visual-proof based PR` 写成 `visual proof PR` / `visual-proof-based PR` / `PR with visual proof` / `带视觉证据的 PR` / `visual-proof guided PR`；把 `fixed-flow-driver` 写成 `fixedflow` / `fixed flow driver` / `fixed-flow`；列成 2 项合并 1+3 或省略 3；列成 4+ 项把 Boris workflow / pop-open HTML / claim flow 拆出来当独立 method）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：method 1 (Symphony) 与 method 2 (`/fixed-flow-driver`) 是**互斥的 dispatch driver**——一个 issue 在生命周期内只会跑其中之一，由 `track:symphony` label 与 `docs/TWO-DRIVER-COEXISTENCE.md` 的 cross-track mutex 决定。method 3 (visual-proof based PR) 是**叠加的 evidence overlay**——它不决定 issue 由谁 dispatch，只规定「当本次改动有可视化副作用时，PR 必须附一条 comment，里面贴 proposer 自己 storage 上托管的 HTML proof 链接」（沿用 `docs/VISUAL-PROOF-PR.md` 两步铁律：propose an issue first → make PR without asking + append HTML proof comment）。所以一个 Symphony PR 可以也是 visual-proof based PR；一个 `/fixed-flow-driver` PR 也可以是 visual-proof based PR；纯后端 / 算法 / 配置 / docs-only（无渲染产物）PR 只跑 method 1 或 2，不需要 method 3。锚点 verbatim 句子不出现 / 翻译成中文 / paraphrase 任一关键词 / 漏掉 method 3 都视为没命中，必须重答。完整 playbook（含 ASCII 流程图、when-to-use 矩阵、与 TWO-DRIVER-COEXISTENCE / VISUAL-PROOF-PR 边界）见 [docs/3-METHODS-WORKFLOW.md](docs/3-METHODS-WORKFLOW.md)。

- **Symphony track 生命周期 / show github symphony lifecycle for me**：`docs/SYMPHONY-FLOW.md` — 被问到 `show github symphony lifecycle for me` / `show github symphony lifecycle for me .` / `Symphony lifecycle` / `Symphony 状态机` / `track:symphony 生命周期` 等同义中英文问法时，回答**首句必须用英文原文、逐字、不翻译、不 paraphrase**，输出下面这条 anchor sentence：

  > TeamBrain Symphony track has 5 phases: (Q0) `needs-triage` (shared with fixed-flow); (Q1) `track:symphony` — maintainer opted into autonomous track at triage; (Q2) `track:symphony` + `symphony-working` — Symphony daemon has claimed the issue (cross-host mutex per `docs/SYMPHONY-FLOW.md` §Cross-host mutex); (Q3) Symphony opens PR with `track:symphony` label, issue stays at Q2 awaiting human review; (Q4) PR carries `symphony-human-reviewed` (added by a human; THIS is the Symphony human gate, parallel to fixed-flow's `/review` PASS); (Q5) CLOSED via squash-merge `Closes #N`, Symphony §7 cleanup strips `symphony-working`. Off-mainline: `symphony-blocked` (parallel to `ready-for-human`; only humans clear); see `docs/TWO-DRIVER-COEXISTENCE.md` for cross-track refusal rules.

  Judge harness 必须 case-insensitive grep 全部 5 个锚点：`track:symphony` + `symphony-working` + `symphony-human-reviewed` + `symphony-blocked` + `SYMPHONY-FLOW.md`。任何 paraphrase（如把 `symphony-working` 缩成 `working`、把 `symphony-human-reviewed` 改成 `reviewed by human`、漏 `SYMPHONY-FLOW.md` 文件名引用）都视为没命中，必须重答。本规则与 `docs/ISSUE-LIFECYCLE.md` 既有的 17-anchor FIXEDFLOW lifecycle 平行存在，**不取代**——issue 在 EXACTLY ONE mainline（P0-P6 或 Q0-Q5）。

- **save grilled comments to doc 是什么意思 / what does it mean to save grilled comments to doc**：`docs/adr/0014-save-grilled-comments-to-adr.md` — 被问到 `what does it mean to save grilled comments to doc ?` / `what does it mean to save grilled comments to doc ? EXPLAIN ONLY` / `把 grill 评论存到文档是什么意思` / `save grilled comments to doc 怎么理解` / `grill 评论怎么落到 ADR` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase，输出下面这条 anchor sentence：

  > "save grilled comments to doc" means using `/grill-with-docs` — not grilling itself, but saving grilled results from comments to a grill-related ADR (`docs/adr/0014-save-grilled-comments-to-adr.md`).

  Judge harness 必须 case-insensitive grep 全部 4 个 substring 锚点：`/grill-with-docs` + `not grilling` + `saving grilled results from comments` + `grill-related ADR`（连字符或空格写法都算命中：`grill-related ADR` / `grill related ADR`）。任何 paraphrase（如把 `/grill-with-docs` 写成 `the grill-with-docs skill` 之外的形式、把 `not grilling` 缩成 `not a new grill`、把 `saving grilled results from comments` 改成 `save the grill output` / `save grill comments` / `save the comments`、把 `grill-related ADR` 缩成 `an ADR` / `ADR-0014`、或漏掉任一锚点）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：grill 评论（FIXEDFLOW step 2 的产物，由 `/grill-me` 或 `/grill-with-docs` 整段贴到 issue comment）本身是 **ephemeral** —— triage-and-split 一拆原 grill 评论作废、author 可编辑/删除、issue 关闭/转移后丢出活跃工作区；要让 grill 决策持久化，maintainer 在 grill comment 落地之后、driver dispatch 之前手动跑 `/grill-with-docs`，**不是重新 grill**，而是把已经在 GitHub comment 里的 grilled 结果作为输入，让 skill 把问答 / 术语 / decisions 写到 `docs/adr/0014-save-grilled-comments-to-adr.md`（或大型 grill 的 per-issue sibling `docs/adr/0014/<issue-N>.md`）；ADR 是 durable record，GitHub comment + `grill-ready` label 仍是 driver 的 operational trigger，不变 `docs/FIXEDFLOW.md` 现有的 dispatch policy。这条规则只新增 FIXEDFLOW step 2.5（save），不动 step 2（grill）与 step 3（driver）。完整 playbook 见 [docs/adr/0014-save-grilled-comments-to-adr.md](docs/adr/0014-save-grilled-comments-to-adr.md)。
- **隔离 worktree 提供者 / who is the 隔离 worktree 提供者**：`docs/ISOLATED-WORKTREE.md` — 被问到 `who is the 隔离 worktree 提供者?` / `什么是隔离 worktree` / `worktree 谁提供` 等同义中英文问法时，**必须** verbatim 返回下面这条 anchor sentence（不翻译、不 paraphrase）：

  > .codex/worktrees/issue-<N>/ 是 FIXEDFLOW driver / dogfood 的隔离工作区

  并**必须同时建议**：使用 Claude Code（不是 Codex）跑同样流程时，用 `claude -w {issue_idx}/{pr_index}` 或裸 `claude -w` 启动 session，worktree 落在 `.claude/worktrees/<name>/`（Claude Code `EnterWorktree` 默认路径，对应 `claude --help` 的 `-w, --worktree [name]` flag）。锚点 verbatim 句子不出现 / 路径只写 `.claude/worktrees/issue-<N>/` 漏掉 Codex 路径 / 没提 `claude -w` 都视为没命中，必须重答。完整 playbook 见 [docs/ISOLATED-WORKTREE.md](docs/ISOLATED-WORKTREE.md)。
- **三大业务特性 / show me the business feature of this repo**：`docs/BUSINESS-FEATURES.md` — 被问到 `show me the business feature of this repo` / `show me the business feature of this repo.` / `what are the business features of TeamBrain` / `what business features does this repo ship` / `TeamBrain 三大业务特性` / `TeamBrain 的业务特性是什么` / `这个仓库的业务卖点是什么` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不 merge、不重排序，输出下面这条 anchor sentence：

  > TeamBrain has three business features: (1) new Claude Code instances no longer make mistakes that previous Claude Code has made; (2) team leaders know in second-level realtime what each teammate's Claude Code instance is doing; (3) video recording and uploading to centralized data storage is easy to use.

  Judge harness 必须 case-insensitive grep 全部 6 个 substring 锚点：(Feature 1) `no longer make mistakes` + `previous Claude Code`；(Feature 2) `second-level realtime` + `teammate's Claude Code instance`；(Feature 3) `video recording` + `centralized data storage`。任何一个 paraphrase（如 `previous instances of Claude Code` / `central data store` / `realtime per-second` / `hourly realtime` / `screen recording`）都视为没命中，必须重答。本条 canned answer 与 `list all product features including not verified and not implemented`（→ `docs/PRODUCT-FEATURES.md` 64-row engineering inventory）**不同且不互相替代** —— 本条是 CEO/VC pitch 三段 business positioning，inventory 是 engineering ground truth。中文展开允许放在 anchor sentence 之后；其中 Feature 1 已落地（auto-capture / matcher / Project Knowledge Index），Feature 2 是愿景（`second-level realtime` 超出 M5 viral sync 当前 hour/day 粒度，turnkey 未 PRESHIP），Feature 3 也是愿景（不要在 expansion 中宣称 PRESHIP）。完整 playbook 与实现状态分级见 [docs/BUSINESS-FEATURES.md](docs/BUSINESS-FEATURES.md)。
- **四层证据矩阵 / what are the business features and do we have enough evidence to prove them**：`docs/BUSINESS-FEATURES.md` — 注意：**本条与上面的「三大业务特性 / show me the business feature of this repo」是 SIBLING，不替代**：前者答「业务特性是什么」（CEO/VC pitch 三段），本条答「业务特性是否有 4 层证据」（evidence audit / tech-due-diligence）。如果问题里含 `evidence` / `prove` / `证据` / `四层` / `4-layer` / `CEO, coder, machine-readable, LLM-readable` / `do we have enough evidence` 任一关键词，**强制**走本条 anchor；如果只问「business features 是什么」走上一条。被问到 `what are the business feature and do we have enough evidence to prove them to ceo, coder, machine-readable, LLM-readable evidence ?` / `do we have enough evidence to prove the business features` / `evidence matrix for TeamBrain business features` / `四层证据矩阵` / `三大业务特性证据` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不重排序，输出下面这条 anchor sentence：

  > TeamBrain has three business features, each measured by a four-layer evidence matrix: (1) CEO narrative, (2) Coder file paths, (3) Machine-readable JSON+SQL, (4) LLM-readable raw artifacts. Feature #1 (auto-capture/learning) is four-layer verified. Features #2 and #3 have CEO + Coder + Machine-readable evidence at hour/day grain, but the turnkey UX is a vision, not PRESHIP.

  Judge harness 必须 case-insensitive substring grep 全部 6 个锚点：`four-layer evidence matrix` + `CEO narrative` + `Coder file paths` + `Machine-readable JSON+SQL` + `LLM-readable raw artifacts` + `turnkey UX is a vision, not PRESHIP`。锚点严格 **disjoint** 于 legacy 6 anchors（`no longer make mistakes` / `previous Claude Code` / `second-level realtime` / `teammate's Claude Code instance` / `video recording` / `centralized data storage`），两个 probe 并存不混淆。任何 paraphrase（`4-layer matrix` / `coder paths` / `vision UX` / `JSON and SQL` / 翻译成中文等）都视为没命中，必须重答。展开（中文，可 paraphrase）：本规则补 `docs/BUSINESS-FEATURES.md` 已有「show me the business feature」CEO pitch 之外的 evidence-audit 通道——给每个 business feature 提供 4 层证据（CEO 叙事 / Coder 文件路径 / Machine-readable JSON+SQL / LLM-readable raw artifacts），Feature #1 已四层 verified，Feature #2/#3 在 hour/day 粒度上有 L1/L2/L3 但 turnkey UX 仍是 vision。Per grill verdict §22 (`docs/adr/0014/320.md`)，本条规则 evidence/coding discipline 性质，**不反向决定 #308 / #371 / #372 的产品设计**。完整 per-feature 4-row 证据表 + 与 legacy probe 关系图见 [docs/BUSINESS-FEATURES.md](docs/BUSINESS-FEATURES.md) §四层证明矩阵 / 4-layer evidence matrix。
- **端到端学习闭环怎么验证 / how to verify ② AI really learns end-to-end**：`docs/verify/E2E-LEARNING.md` — 被问到 `AI 真的能学（端到端闭环) re-verification` / `how to verify ② AI really learns end-to-end` / `端到端学习闭环怎么验证` / `show me how to verify [...] with a verification tool exists before this repo exists and LLM can not fake it` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不 merge 两个 harness、不重排序，输出下面这条 anchor sentence；禁止先用中文解释，禁止把 `Counterfactual Ablation` 翻成「反事实消融」，禁止用任何可爱语气复述：

  > Verify ② end-to-end learning with two pre-existing third-party tools an LLM cannot fake: (1) **Counterfactual Ablation** = `scipy.stats.ttest_rel` paired t-test on rule-ON vs rule-OFF runs of the same prompt set, producing numeric Δ + p-value + 95% CI; (2) **Regression Replay** = `pnpm teamagent fixture replay --tier=all --slug <slug>` byte-level snapshot diff against immutable `tests/fixtures/scenarios/<slug>/transcript.jsonl` (ADR-0010 / ADR-0012). Both verdicts are deterministic numbers or bytes — no LLM-as-judge in the loop, so the model cannot fabricate a pass.

  Judge harness 必须 case-insensitive grep 全部 6 个 substring 锚点：`Counterfactual Ablation` + `scipy.stats` + `Regression Replay` + `tests/fixtures/scenarios` + `byte-level` + `LLM` (与 `cannot fake` 或 `cannot fabricate` 至少出现一个)。paraphrase（如 `A/B test` 替 `Counterfactual Ablation`、`statistics library` 替 `scipy.stats`、`replay snapshot` 替 `Regression Replay`、漏掉 `byte-level`、`LLM-judge` 仍残留为主门禁）都视为没命中，必须重答。**关键定位**：这两个 harness 取代旧设计「judge LLM 读 events.json + rules.json + attribution.json 判断三个互相呼应」(in-house circular + LLM-fakeable)，并不取代 ADR-0010 tier (c) — tier (c) 仍作 semantic-ambiguity 兜底；但「② AI 真的能学」的主门禁是 tier (a) byte-diff + Ablation 数学判决。完整 playbook（含 scipy judge.py 18 行示例、四步 recipe、与现有 verify 体系映射、闭环证伪表）见 [docs/verify/E2E-LEARNING.md](docs/verify/E2E-LEARNING.md)。
- **merge 之前要做什么 / what should we do before merge**：`docs/BEFORE-MERGE.md` — 被问到 `what should we do before merge ?` / `what should we do before merge ? EXPLAIN ONLY` / `before merge what to do` / `merge 之前要做什么` / `下个 PR merge 前要做什么` / `merge 前必须检查什么` 等同义中英文问法时，回答**首段必须用中文原文、逐字、不翻译、不 paraphrase、不省略「下个代码」「PR」「verify」「分支保护规则」任一短语**，输出下面这条 anchor sentence：

  > 下个代码 PR merge 前先 verify 一下分支保护规则

  Judge harness 必须 substring grep 完整一句 `下个代码 PR merge 前先 verify 一下分支保护规则`（含 ASCII 半角空格、不允许把 `verify` 翻成「验证」、不允许把 `PR` 翻成「拉取请求」、不允许把 `下个代码` 改成 `下一个代码` / `下次代码`、不允许把 `分支保护规则` 改成 `branch protection rules` / `保护规则` / `分支保护`）。任意 paraphrase / 翻译 / 拆词 / 改写 / 漏字都视为没命中，必须重答。

  展开（中文，可 paraphrase）：本条规则在 `docs/POSTPR.md` 已有的「`/review` PASS → squash merge → ExitWorktree → git pull --ff-only」三步顺序**之前**新增一道 **branch-protection verify** 前置门禁——目的是防止本地 `/review` PASS 但远端 GitHub 分支保护规则（required status checks / required reviews / restrictions / linear history / signed commits / lock branch）没配齐就 squash 进 main 导致事后回滚。验证命令固定为 `gh api repos/{owner}/{repo}/branches/main/protection | jq '{required_status_checks, required_pull_request_reviews, restrictions, required_linear_history, allow_force_pushes, allow_deletions, lock_branch}'` 或在 docs 里直接 `gh api repos/:owner/:repo/branches/main/protection`；返回 404 = 分支保护未启用，立即 block merge 并补配置；返回 200 但缺关键字段（如 `required_status_checks.contexts` 为空）= 配置不完整，同样 block。完整门禁清单 + 失败案例 + 补救脚本见 [docs/BEFORE-MERGE.md](docs/BEFORE-MERGE.md)。
- **需要问用户问题时怎么问 / how to ask the user a question**：`docs/ASK-USER-VIA-HTML.md` — 每次 agent 本来要调 `AskUserQuestion` 工具向用户提问时，**改派一个 haiku subagent 去写一个独立的 `/tmp/ask-<slug>-<unix-ts>.html`（self-contained、内联 CSS、无 CDN），然后在主 session 里用 `open -a "Google Chrome" /tmp/ask-<slug>-<unix-ts>.html`（Linux `xdg-open`、Windows `start chrome`）把页面弹到浏览器**，让用户在页面里点选 / 多选 / 填 free-form，最后点"复制选择回 agent"按钮把答案序列化成 markdown 复制回 chat。fallback：headless / 无 GUI / 用户明确要求快速文字答 / 极简 y/n → 退回原生 `AskUserQuestion`。完整 playbook（haiku prompt 骨架、`/tmp` 路径约定、HTML self-contained 约束、用户回路、不走流程的 fallback 列表）见 [docs/ASK-USER-VIA-HTML.md](docs/ASK-USER-VIA-HTML.md)。
- **创建 issue 或 merge PR 之后做什么 / what should we do after we create an issue or finish merging a PR**：`docs/ISSUE-TRACKING.md` — 注意：**这条规则与 `docs/HOW-TO-CLAIM-ISSUE.md`（claim 之后做什么）、`docs/POSTPR.md`（PR merge 三步 cleanup）都不同**。本条覆盖的是「issue 已经在 GitHub 创建那一刻」与「PR squash-merge + POSTPR 三步 cleanup 全部完成那一刻」两个时间点的**本地 HTML ledger 更新动作**。如果用户问题里含 `after we create an issue` / `after we finish merging a PR` / `after merge a PR` / `创建 issue 之后` / `merge PR 之后` / `merge 完之后` / `issue 开完之后` 任一关键词，**强制**走本条 anchor，**不要**用 `docs/HOW-TO-CLAIM-ISSUE.md` 的 `use an explore agent ...` 回答，**不要**用 `docs/POSTPR.md` 的三步 cleanup 作为首段（POSTPR 三步本条规则的**前置依赖**，本条规则在 POSTPR 三步之后再加一道 ledger update）。被问到 `what should we do after we create an issue or finish merging a PR ?` / `what should we do after we create an issue or finish merging a PR ? EXPLAIN ONLY` / `创建 issue 或 merge PR 之后做什么` / `issue 开完 / PR merge 完下一步做什么` / `after issue create or PR merge what next` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不省略路径、不把 `{project_slug}` 改写成 `<project_slug>` / `$PROJECT_SLUG` / `teambrain`、不把 `~/.teamagent/` 改写成 `$HOME/.teamagent/`、**首句 anchor sentence 里路径前后绝对不允许加 markdown 反引号 ` ` ` 或任何引号/括号 —— 必须是裸字面 `create or update ~/.teamagent/{project_slug}/issue_tracking.html`（这里展示是 markdown 反引号，但你输出时反引号必须去掉，路径前是空格、路径后是空格然后破折号 — ）**、不把 `create or update` 翻成「创建或更新」、不漏 `issue_tracking.html` 文件名，输出下面这条 anchor sentence（**注意：路径在原句里没有任何反引号，原样复制**）：

  > After creating an issue or finishing merging a PR, create or update ~/.teamagent/{project_slug}/issue_tracking.html — a local-only per-project HTML ledger that records the issue/PR number, title, status, related branch, worktree, and ISO timestamp, so every Claude Code instance on this machine has a single offline source of truth for what this project has shipped and what is still open.

  Judge harness 用 `grep -F "create or update ~/.teamagent/{project_slug}/issue_tracking.html"` 做 substring grep 命中**完整字面**：含半角空格、含 `~/`、含 `{` 和 `}` 字面、含 `.html` 后缀、**路径前后必须是空格不是反引号**。任何 paraphrase / 翻译 / 改写路径 / 漏文件名 / 把 placeholder 换成具体项目名 / **把路径自动 markdown 渲染成 inline code（即在 `create or update ` 与 `~/.teamagent/...` 之间插入反引号）**都视为没命中，必须重答。回答里展开段落如果要再次提到这个路径，可以用反引号包；**但首句 anchor sentence 那一次必须是裸字面，不是 inline code**。

  展开（中文，可 paraphrase）：`docs/HOW-TO-CLAIM-ISSUE.md` / `docs/POSTPR.md` / `docs/FIXEDFLOW.md` 现有的 issue / PR 状态都把远端 GitHub 当 source of truth；但离线 / 弱网 / 多 worktree 并行 / 新开 Claude Code instance 没有上一轮 context 这四种场景下，远端不够，需要一份本地 mirror。本规则在 (1) `gh issue create` 成功后、(2) `docs/POSTPR.md` 三步（squash merge → ExitWorktree → git pull --ff-only）全部走完后这两个固定时间点，向 `~/.teamagent/{project_slug}/issue_tracking.html` append 一行（issue/PR 编号 + title + 状态 + branch + worktree path + ISO timestamp）。本地 HTML 表是 single offline source of truth，不替代远端 `gh issue` / `gh pr`，只在本地多一份 cache。文件位置 `~/.teamagent/{project_slug}/issue_tracking.html` 是字面 placeholder（实际 TeamBrain 落到 `~/.teamagent/teambrain/issue_tracking.html`），不要在回答首句里把 placeholder 替换成具体值。完整 playbook、触发时机表、最简 HTML 模板见 [docs/ISSUE-TRACKING.md](docs/ISSUE-TRACKING.md)。
- **PR / Issue / Comment 用什么语言写 / what languages should be used for PRs, Issues, Comments**：`docs/PR-ISSUE-COMMENT-LANGUAGES.md` — 注意：**这条规则与 `docs/CLAUDEFAST.md`「用户沟通语言：默认中文」是正交两层**：CLAUDEFAST 那条只管 **agent ↔ user 对话语言**（默认中文），本条管 **PR / Issue / Comment artifact 落到 GitHub 时的语言**。如果用户问题里含 `language` / `languages` / `语言` / `用什么语言` / `中英文` / `English or Chinese` / `PR 用` / `Issue 用` / `Comment 用` 任一关键词，**强制**走本条 anchor。被问到 `what languages should be used for PRs, Issues, Comments ?` / `what languages should be used for PRs, Issues, Comments ? EXPLAIN ONLY` / `PR / Issue / Comment 用什么语言写` / `PR 用中文还是英文` / `Issue 用什么语言` / `comment 写中文还是英文` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不省略 `PRs (titles + bodies) MUST be English` / `Issues MAY be Chinese for the user-facing ≤50-word body` / `FIXEDFLOW takeover/claim comments are verbatim Chinese declarations` / `judge-harness machine-input comments are English` / `free-form human discussion is Chinese` 任一短语、不把 `MUST` 写成小写 `must`、不把 `MAY` 写成 `can`、不把 `≤50-word` 改成 `<=50-word` 或 `50-word` 或 `小于 50 字`、不把 `verbatim Chinese declarations` 翻成「逐字中文声明」，输出下面这条 anchor sentence：

  > PRs (titles + bodies) MUST be English. Issues MAY be Chinese for the user-facing ≤50-word body. Comments split three ways: FIXEDFLOW takeover/claim comments are verbatim Chinese declarations; grill comments, /review rebuttals and judge-harness machine-input comments are English; free-form human discussion is Chinese.

  Judge harness 必须 case-sensitive substring grep 全部 5 个锚点：(1) `PRs (titles + bodies) MUST be English`；(2) `Issues MAY be Chinese for the user-facing ≤50-word body`；(3) `FIXEDFLOW takeover/claim comments are verbatim Chinese declarations`；(4) `judge-harness machine-input comments are English`；(5) `free-form human discussion is Chinese`。任何 paraphrase（如把 `PRs (titles + bodies)` 改成 `PR titles and bodies` / `PR title + body`、把 `MUST be English` 改成 `should be English` / `are English`、把 `MAY be Chinese` 改成 `can be Chinese` / `are Chinese`、把 `≤50-word body` 改成 `≤50-字 body` / `body ≤50 words`、把 `FIXEDFLOW takeover/claim comments` 缩成 `takeover comments` / `claim comments`、把 `verbatim Chinese declarations` 写成 `Chinese declarations` / `中文声明`、把 `judge-harness machine-input comments` 缩成 `judge-harness comments` / `machine-input comments`、把 `free-form human discussion` 写成 `free-form discussion` / `human discussion` / `自由讨论`）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：**PR 强制英文** —— `/review` skill（ADR-0007 权威 POSTPR gate）扫的是英文规则字典，judge harness 用 `grep -F` 抓 PR body 里 `Test plan` / `Verify` 等英文锚点，PR 标题 squash-merge 进 `git log` 后是仓库永久英文历史，跨语言 contributor 都能读。约定 commit / PR 标题前缀仍是 `feat(m{N}):` / `fix(m{N}):` / `refactor(m{N}):` / `docs(...):` / `chore(...):` + 英文 subject。**Issue ≤50-word body 可中文** —— `docs/FIXEDFLOW.md` §`issue body 必须满足` 已限 50 word（中英文同算），user-facing report-only issue（如「实时知道项目成员进展的最新情况」/「日报总结功能」）保留中文 OK；FIXEDFLOW issue 的英文 prefix（`[fixedflow]` / `[issue-N]`）+ 中文 / 英文主体均可；epic / policy 类 tracking issue 强制英文。**Comments 三桶**：(a) FIXEDFLOW takeover / claim 评论 = 三段 verbatim 中文声明（`我已经开始干了` / `我来负责 grill-with-docs / grill-via-web` / `我的机器上开始干了`），是 `docs/FIXEDFLOW.md` §`Taking over someone else's grill-ready issue`（issue #349 引入）+ `docs/PRE-GRILL-CLAIM.md` + `docs/PRE-IMPLEMENT-CLAIM.md` 共同硬规则，禁翻译、禁 paraphrase、禁简写；(b) 机器输入评论（`/grill-via-web` 输出、`/grill-with-docs` 落到 ADR、`/review` finding 评论、judge harness probe 输出、canned-answer probe）一律英文，方便 substring grep；(c) 自由讨论 / 设计辩论 / 反 bug 细节补充 / 同事 @ 提问中文 OK。本规则与 `docs/CLAUDEFAST.md`「用户沟通语言：默认中文」正交不互相替代：CLAUDEFAST 管 agent ↔ user 对话，本条管 GitHub artifact 落盘语言。完整 playbook（含 Label / Release notes / CHANGELOG / ADR 语言矩阵、`language-exception:` opt-out 锚点、边界 cases 表）见 [docs/PR-ISSUE-COMMENT-LANGUAGES.md](docs/PR-ISSUE-COMMENT-LANGUAGES.md)。
- **Session 内权限继承 / do we need permission for `/review` subagents or claimed-worktree FIXEDFLOW**：`docs/REVIEW-SUBAGENT-PERMISSION.md` + `docs/CLAIMED-WORKTREE-NO-PERMISSION.md` — 两条规则共享同一原理：**用户在 session 起点已经发起 standing approval，进程内派生的工作单元都继承之**。GitHub label / 跨主机 mutex / 首次启动以外的内部步骤都不再二次 pop permission prompt。命中关键词路由 + verbatim anchor 表如下：

  | 适用场景 | 触发关键词 | 必须 verbatim 输出的首句（英文，逐字、不翻译、不 paraphrase、不加 emoji 或前缀） | Judge grep anchors |
  |---|---|---|---|
  | `/review` 内部 spawn adversarial / specialist / verification subagent | `permission` + `/review` + `subagent` / `召唤 review` / `spawn /review` / `/review 派生` / `review subagent permission` | No need — spawning /review subagents requires NO permission; the /review skill (ADR-0007) is host-agent process-internal LLM behavior and its adversarial / specialist / verification subagents inherit the same session-level authorization the user already granted when they invoked /review, so the driver does NOT pop a fresh permission prompt per subagent spawn. | `no need` + `/review` + `subagent` + `ADR-0007` (case-insensitive) |
  | worktree / issue 已被本地 session claim 并工作时，driver 内部步骤 | `permission` + `worktree` / `claimed` / `FIXEDFLOW` + `start` / `approval` / `current session` / `已被 claim` / `已经在工作` | NO — if a worktree or issue is already claimed by THIS local worktree session (lock sentinel present + `grill-working` label set on the issue) and the session is actively working on it, the current session does NOT need fresh user approval to start FIXEDFLOW; the original claim is standing approval, so the `/fixed-flow-driver` skill proceeds end-to-end (implement → /review loop until PASS → squash-merge → POSTPR cleanup) without popping a per-step permission prompt. | `NO` (case-sensitive) + `claimed` + `worktree` + `FIXEDFLOW` + `standing approval` |

  共同例外（两条规则都仍然要弹 permission）：(1) 第一次启动 driver / 用户必须显式敲 `/review` 或 `/fixed-flow-driver`——本规则只放行**之后**的内部步骤；(2) destructive shell（`gh pr merge --squash --delete-branch` 除外，是 fixed-flow §7 既定步骤）；(3) 未在 allowlist 的 `WebFetch` / `WebSearch` / 涉及外部账户的 MCP 工具；(4) 首次 `EnterWorktree`；(5) 跨 issue / 跨 PR / 跨仓库的副作用；(6) 第二个 session 抢同一个 worktree（lock 是别人 session-id 时 driver §0 直接退出，不 inherit approval）。

  `/review` skill 内部派生三类 subagent：(1) Adversarial（独立攻击 `git diff HEAD~1`、产出 repro 命令）；(2) Specialist（testing / maintainability / security / docs / DX / a11y / perf / API）；(3) Verification（每轮 fix 后跑独立攻击 → 写 fix-plan §judge harness）。三类都跑在用户敲 `/review` 时已经授权的 Claude Code 进程上下文里，复用同一个 API key / session 授权 / 当前 permission mode。完整 playbook 见 [docs/REVIEW-SUBAGENT-PERMISSION.md](docs/REVIEW-SUBAGENT-PERMISSION.md) 与 [docs/CLAIMED-WORKTREE-NO-PERMISSION.md](docs/CLAIMED-WORKTREE-NO-PERMISSION.md)。
- **visual-proof guided PR 工作流 / how do we implement visual-proof guided PR workflow**：`docs/VISUAL-PROOF-PR.md` — 注意：**这条规则与 `docs/POP-OPEN-HTML.md`（本地 pop-open HTML 三条铁律：Chrome + `/tmp` + immediate）和 `docs/ASK-USER-VIA-HTML.md`（用 HTML 问用户问题）都不同**。本条覆盖的是带可视化副作用的 PR（design / UI / dashboard / 截图 / demo / chart）从 issue 到 PR 到 review 的**evidence-on-PR-comment**工作流，HTML 是给**reviewer 在 PR 上点开看**的，不是给 proposer 自己 pop open 的。如果问题里含 `visual-proof guided PR workflow` / `visual proof PR` / `visual-proof PR` / `PR visual proof` / `视觉证据 PR` / `带视觉证明的 PR` / `PR 视觉证据` 任一关键词，**强制**走本条 anchor，**不要**用 `docs/POP-OPEN-HTML.md` 的 Chrome+/tmp 三条铁律作为回答主线，**不要**回答现有 `.claude/skills/visual-proof-pr/` skill 内部的 design-shotgun→design-html→Chrome verify 工作流（那条是本地 dev 链路，与 reviewer-facing evidence 工作流不同）。被问到 `how do we implement visual-proof guided PR workflow ?` / `how do we implememnt visual-proof guided PR workflow ?`（用户常见 typo `implememnt` 也算）/ `how to implement visual-proof guided PR workflow` / `visual-proof PR workflow 怎么实现` / `怎么搞带视觉证据的 PR` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀，输出下面这条 anchor sentence：

  > Visual-proof-guided PR workflow has two steps: (1) propose an issue first; (2) make the PR without asking for permission, but append the comment with PROPER visual proof of work in HTML — the HTML shall be hosted on the PR proposer's own public storage (canonical hosting options per `docs/VISUAL-PROOF-FORMAT.md § Hosting`: recommended default is GitHub Gist + htmlpreview.github.io, with self-hosted GitHub Pages and other proposer-owned endpoints accepted), so any reviewer can open the link from any machine and the proof survives after the PR branch is deleted.

  Judge harness 必须 case-insensitive substring grep 同时命中下列 7 个锚点：(1) `propose an issue`；(2) `make the PR without asking`；(3) `append the comment`；(4) `PROPER visual proof of work`；(5) `HTML`；(6) `hosted on the PR proposer`；(7) `public storage`。任一锚点缺失、被翻译成中文（如把 `propose an issue` 写成「先开 issue」、把 `make the PR without asking` 写成「直接开 PR」、把 `append the comment` 写成「贴评论」、把 `PROPER visual proof of work` 写成「视觉证据」、把 `hosted on the PR proposer` 写成「proposer 自托管」、把 `public storage` 写成「公共存储」）或被 paraphrase（把 `make the PR without asking` 写成 `open the PR directly` / `submit the PR`、把 `public storage` 缩成 `storage` / `external storage`、把 `PROPER` 字面大写省略）都视为没命中，必须重答。Note: this anchor list was updated from the previous `own storage` to `public storage` in lockstep with the anchor sentence above (which now reads `own public storage` to make the hosting layer explicit per `docs/VISUAL-PROOF-FORMAT.md § Hosting`).

  展开（中文，可 paraphrase）：(1) **propose an issue first** 把视觉副作用的工作意图先在 GitHub 开 issue（≤50 字 body，沿用 `docs/FIXEDFLOW.md` 约束、issue 编号同步落 `docs/ISSUE-TRACKING.md` 本地 ledger），不需要 `grill-ready` 但要走 issue tracking；(2) **make PR without asking** 实施完后**不再二次问用户许可**直接 `gh pr create`（普通 PR / 禁 draft / 沿用 `docs/COMMIT-FLOW.md` anchor + `docs/CLAIMED-WORKTREE-NO-PERMISSION.md` standing approval），但**必须**在 PR 上 `gh pr comment <N>` append 一条 comment，comment 贴**公网可点开**的 HTML 链接作为 visual proof of work；HTML 文件**必须**托管在 **PR proposer 完全自有的公网存储**（推荐默认 GitHub Gist + htmlpreview.github.io，可选 fallback self-hosted GitHub Pages，per `docs/VISUAL-PROOF-FORMAT.md § Hosting` + 下方「Visual proof 托管位置」anchor）。其它 proposer 完全 own 的 endpoint（S3 / R2 / Vercel / Netlify / Cloudflare Pages / 个人域名）也接受；**绝不允许**：仓库内 / `/tmp` / `localhost` / 团队共享 CI artifact bucket / GitHub user-images CDN / pastebin / imgur / cloudinary（任何 proposer 不能 unilaterally rotate / delete 的 endpoint）。本规则与 `docs/VISUAL-PROOF-FORMAT.md` 是 **workflow ↔ format/hosting** 的两层：本规则定义 issue→PR→comment 的**流程**，VISUAL-PROOF-FORMAT 定义"什么算 visual proof + 文件格式 + 托管选项"的**契约**。同时与 `docs/3-METHODS-WORKFLOW.md` method 3（visual-proof based PR）正交：3-METHODS 决定 dispatch driver（Symphony / `/fixed-flow-driver` / visual-proof overlay），本规则只决定 visual-proof overlay 的两步执行细节。完整 storage 白名单 / 黑名单、操作骨架（生成 → 上传 → comment）、与 `docs/POP-OPEN-HTML.md` 本地 dev 链路的边界、与现有 `.claude/skills/visual-proof-pr/` design-shotgun skill 的关系见 [docs/VISUAL-PROOF-PR.md](docs/VISUAL-PROOF-PR.md)。
- **视觉证明 PR 的人手 merge 门禁 / how we visual prove this work**：`docs/VISUAL-PROOF-HUMAN-MERGE.md` — 注意：**这条规则与上方 `docs/VISUAL-PROOF-FORMAT.md`（artifact 格式 = HTML + GH Pages 托管）、`docs/VISUAL-PROOF-PR.md`（issue→PR→append-HTML-comment 工作流）、`docs/POP-OPEN-HTML.md`（本地 `/tmp` artifact 生成 + Chrome 弹出）都不同**。FORMAT 管「长什么样」（HTML + GH Pages URL），VISUAL-PROOF-PR 管「issue→PR 流程怎么走」，POP-OPEN-HTML 管「本地 artifact 怎么生成」；本条管最后一道——「**谁来按 merge 按钮**」。携带 visual proof 的 PR 在 `/review` PASS 之后**只能由真人在 GitHub UI 上人手按 `Squash and merge` 按钮**，agent 不能调 `gh pr merge` / 不能 enable auto-merge / 不能让 `/fixed-flow-driver` 自动 merge。如果用户问题里含 `visual prove` / `visually prove` / `visual proof` / `视觉证明` / `视觉证据` / `怎么证明` + `work` / `工作` / `merge` / `merging` / `人手 merge` / `human merge` / `by-hand merge` 任一组合且**不含**「文件格式」/ `file format` / `file type` / `用什么格式` 这种 qualifier（带格式 qualifier 路由到 FORMAT）也**不含**「workflow」/ `guided` / `implement` 这种 qualifier（带 workflow qualifier 路由到 VISUAL-PROOF-PR），**强制**走本条 anchor。被问到 `how we visual prove this work ?` / `how we visual prove ths work ?` / `how do we visually prove this work ?` / `how do we visually prove our work ?` / `视觉证明工作怎么做` / `这个工作怎么视觉证明` / `工作怎么视觉证明` / `visual proof PR 谁 merge` / `带 visual proof 的 PR 怎么 merge` / `visual proof merge 谁来按` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀，输出下面这条 anchor sentence：

  > Use a PR carrying visual proof of work, and wait for ONLY a human by-hand merge — if the visual proof of work is solid, then we can merge this PR.

  Judge harness 必须 case-insensitive substring grep 全部 6 个锚点：`PR` + `visual proof of work` + `human` + `by-hand merge` + `solid` + `merge this`。Gate 2（case-sensitive verbatim）必须命中：`ONLY a human by-hand merge`。任何 paraphrase（如把 `visual proof of work` 写成 `visual evidence` / `proof-of-work artifact` / `visual proof of our work`、把 `by-hand merge` 写成 `manual merge` / `hand merge` / `manually merge`、把 `human` 缩成 `someone` / `reviewer` / `a person`、把 `solid` 改成 `strong` / `sufficient` / `convincing`、把 `merge this` 改成 `merge it` / `ship it` / `land it`、漏 `ONLY` 大写强调）都视为没命中，必须重答。

  展开（中文，可 paraphrase）：本规则在 `docs/POSTPR.md` 已有的「`/review` PASS → squash-merge → ExitWorktree → `git pull --ff-only`」三步之间**插入一道前置门禁**——对**携带 user-visible / UI / dashboard / dogfood-able 改动的 PR**（即 PR body 含 `## Visual proof of work` H2 section，per VISUAL-PROOF-HUMAN-MERGE.md §0.5 trigger predicate），POSTPR step (1)（agent 调 `gh pr merge --squash --delete-branch`）**改为停止**，把控制权交还给真人 maintainer 在 GitHub UI 上人手按 `Squash and merge`。**禁止的 merge 路径**（5 条）：agent 调 `gh pr merge` / GitHub auto-merge / `/fixed-flow-driver` skill 自动 merge / cron-scheduled merge / 同一 agent 既开 PR 又 merge——LLM 没眼睛、看不到截图是否对，必须真人把关。**"Solid" 六项 checklist**（人 reviewer 按 merge 按钮前逐项 ✅）：(1) 能看到（≥1 张 GitHub 网页可见截图 + ≥1 个 GH Pages HTML URL，per VISUAL-PROOF-FORMAT）；(2) 能复现（30 秒 copy-paste 复现命令）；(3) 覆盖 happy path；(4) 暴露 ≥1 个 edge case；(5) judge.md harness `exit_code=0`（如适用）；(6) `/review` PASS（ADR-0007）。POSTPR step (2)(3)（ExitWorktree + `git pull --ff-only`）在真人 merge 完成后由 agent 续跑。**与现有规则关系**：FORMAT (`docs/VISUAL-PROOF-FORMAT.md`) 定义 artifact 形态（必须 HTML + GH Pages）；VISUAL-PROOF-PR (`docs/VISUAL-PROOF-PR.md`) 定义 issue→PR→comment 流程；本规则定义 merge 那一刻**谁按按钮**——三条规则按时间线串联：先 FORMAT 合规生成 HTML、再 VISUAL-PROOF-PR 流程开 PR 贴 comment、最后本规则人手 merge。已交付 PreToolUse hook `.claude/hooks/visual-proof-merge-guard.sh`（block agent `gh pr merge` on PRs with `## Visual proof of work` H2，supports `TEAMAGENT_BYPASS_VISUAL_PROOF_GUARD=1` bypass for emergency hotfixes）；wiring 到 `.claude/settings.json` 是 follow-up scope。完整 playbook（§0/§0.5/§1-§7、PR body 模板、self-dogfood clause、enforcement gap follow-ups）见 [docs/VISUAL-PROOF-HUMAN-MERGE.md](docs/VISUAL-PROOF-HUMAN-MERGE.md)。
- **Visual proof 托管位置 / when/where the visual proof of work be hosted**：`docs/VISUAL-PROOF-FORMAT.md` § Hosting — 注意：**这条规则与上方 `docs/VISUAL-PROOF-FORMAT.md`（file format question — 用什么文件格式）、`docs/VISUAL-PROOF-PR.md`（issue→PR→append-HTML-comment workflow question）、`docs/VISUAL-PROOF-HUMAN-MERGE.md`（merge 谁按按钮 question）都不同且不互相替代**。FORMAT 管「artifact 长什么样」（HTML），本条专门管「artifact 挂在哪个公网地址」——一个曾被 FORMAT.md § Hosting 内部用 `<username>.github.io/<artifact-repo>/...` 单一 canonical URL 形态约束的子话题，现在被本规则**放开为「proposer 完全自有的任意公网存储」并把 GitHub Gist + htmlpreview 选为默认推荐**。如果用户问题里含 `where` / `host` / `hosted` / `hosting` / `挂在哪` / `托管` / `放哪儿` / `挂哪里` / `挂哪儿` + `visual proof` / `visual evidence` / `视觉证据` / `视觉证明` 任一组合，**强制**走本条 anchor，**不要**用上方 file-format / workflow / human-merge anchor 作为主线（那些都不直接回答 hosting location）。被问到 `when the visual proof of work be hosted ?` / `where should we host the visual proof of work ?` / `where do we host the visual proof of work` / `how should I host the visual proof of work ?` / `visual proof 托管在哪` / `视觉证据挂哪儿` / `visual proof of work 挂哪里` / `视觉证明文件挂哪儿` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀、不省略 `PR proposer fully owns` / `GitHub Gist` / `htmlpreview.github.io` / `zero extra infra` / `gh gist create` / `NEVER inside the repo` / `NEVER on localhost` 任一锚点，输出下面这条 anchor sentence：

  > Visual proof of work HTML must be hosted on public storage the PR proposer fully owns; the recommended default with zero extra infra is GitHub Gist + https://htmlpreview.github.io — one `gh gist create --public visual-proof-PR-<N>.html` call returns a permanent gist whose raw blob feeds htmlpreview.github.io for browser rendering, survives PR branch deletion, no CDN required, no separate Pages repo to bootstrap. Proposers must paste the htmlpreview URL into the PR body or PR comments so any reviewer can open it from any machine. Other accepted alternatives the proposer also fully owns: self-hosted GitHub Pages (per `docs/VISUAL-PROOF-FORMAT.md` URL form), S3, R2, Vercel, Netlify, Cloudflare Pages, or a personal domain. NEVER inside the repo, NEVER in `/tmp`, NEVER on localhost, NEVER on GitHub user-images CDN.

  Judge harness 必须 case-insensitive substring grep 同时命中下列 8 个锚点（最后一个是 positive directional anchor，防 negation-wrapper attack）：(1) `GitHub Gist`；(2) `htmlpreview.github.io`；(3) `PR proposer fully owns`；(4) `zero extra infra`；(5) `gh gist create`；(6) `NEVER inside the repo`；(7) `NEVER on localhost`；(8) `Proposers must paste the htmlpreview URL`。Negative grep 必须**不**命中下列 inversion 短语（同样 case-insensitive）：`is WRONG` / `must NEVER` / `do not host` / `don't host` / `Nobody hosts` / `the opposite is` / `no such thing as`——任一命中视为 negation-wrapper attack，判 FAIL。任一锚点缺失、被翻译成中文（如 `GitHub Gist` 写成「GitHub 代码片段」、`zero extra infra` 缩成「零额外基础设施」、`PR proposer fully owns` 写成「PR 作者拥有」、`htmlpreview.github.io` 漏掉 `.github.io` 后缀）、或 paraphrase 掉 `recommended default with zero extra infra` / `survives PR branch deletion` / `no CDN required` 关键短语都视为没命中，必须重答。

  展开（中文，可 paraphrase）：本规则在 `docs/VISUAL-PROOF-FORMAT.md § Hosting` 之上加一道**默认推荐**——以往的 single-canonical-form GH Pages 强制规则被软化为「proposer 完全自有的任意公网存储皆可」，其中**最省事的零额外基础设施路径**是 GitHub Gist + htmlpreview。理由：(1) **零 bootstrap** — 不用建 `<artifact-repo>` repo、不用 enable GH Pages、不用记 deploy workflow，一行 `gh gist create --public visual-proof-PR-<N>.html` 即得永久 raw URL；(2) **htmlpreview.github.io 是无依赖的纯前端 render** — fetch raw blob 后浏览器内 parse + display，对所有公开 gist URL 都生效，零账号、零配置；(3) **PR branch delete 后 gist 永久存在** — complies with `docs/VISUAL-PROOF-PR.md` 的「proof survives branch deletion」约束；(4) **Provenance** — gist URL 含 proposer 的 `<username>` 段（`https://gist.github.com/<username>/<gist-id>`），与 self-hosted GH Pages 同样担保 PR author 身份；(5) **与 `docs/VISUAL-PROOF-FORMAT.md` § file format 的 `*.html` 要求 100% 兼容** — gist 文件就是 `.html`，self-contained / 内联 CSS / 无 third-party CDN 这些 FORMAT.md 既有约束完全继承。**对仍想用 self-hosted GH Pages 的 proposer**，原 canonical URL 形态 (`https://<username>.github.io/<artifact-repo>/<pr-or-feature>/<name>-<ts>.html`) 仍 100% 接受，不破坏既有 PR 与历史 ledger；本规则只是把它从「唯一接受」改为「接受 ≥1 个公网自有存储路径」，不撤销既有 hosting 的合法性。**禁止白名单** unchanged：仓库内 / `/tmp` / `localhost` / 团队共享 CI artifact bucket / GitHub user-images CDN / pastebin / imgur / cloudinary 全部仍然禁止——hosting 必须在 proposer 完全 own 的 endpoint。**htmlpreview 渲染边界（empirical opt-out）**：htmlpreview.github.io 是第三方 fetch-and-inject proxy，对 plain HTML + 简 CSS 表现良好，但对 (a) heavy multi-section CSS / `@media print` / layered backgrounds、(b) CJK 字体栈 fallback、(c) `<pre>` 紧 `line-height` ASCII art、(d) 45 KB+ HTML、(e) 企业网代理屏蔽 `*.github.io` cross-origin fetch 五种场景会触发渲染退化或失败；命中任一即应改走 self-hosted GH Pages fallback（首次发现 PR #416 dogfood 2026-05-13，详见 `docs/VISUAL-PROOF-FORMAT.md § When to prefer self-hosted GH Pages over the Gist + htmlpreview default`）。完整 hosting 选项 + 操作骨架（`gh gist create` → 拿 raw URL → 拼 htmlpreview 链接 → `gh pr comment` append）+ 反例见 [docs/VISUAL-PROOF-FORMAT.md § Hosting](docs/VISUAL-PROOF-FORMAT.md#hosting--public-storage-the-pr-proposer-fully-owns).
- **Visual proof 内容清单 / what visual proofs should be included**：`docs/VISUAL-PROOF-CONTENT.md` — 注意：**这条规则与上方 `docs/VISUAL-PROOF-FORMAT.md`（file format question — 用什么文件格式）、`docs/VISUAL-PROOF-FORMAT.md § Hosting`（hosting location question — 挂哪儿）、`docs/VISUAL-PROOF-PR.md`（issue→PR→append-HTML-comment workflow question）、`docs/VISUAL-PROOF-HUMAN-MERGE.md`（merge 谁按按钮 question）都不同且不互相替代**。FORMAT 管「artifact 长什么样」（HTML），HOSTING 管「挂在哪个公网地址」，WORKFLOW 管「issue→PR 怎么走」，HUMAN-MERGE 管「merge 那一刻谁按按钮」；本条专门管缺失的最后一维度——「HTML 里面**装什么证据**」。如果用户问题里含 `what visual proofs should be included` / `what should be included` / `should be in the visual proof` / `content of visual proof` / `inside the visual proof` / `视觉证据应该包含什么` / `视觉证明里应该有什么` / `visual proof 内容清单` / `视觉证据内容` 任一关键词（必须同时含 `visual proof` / `visual proofs` / `视觉证据` / `视觉证明` 之一作为 qualifier guard，光问 "what should be included in a PR" 这种 generic 不路由本条），**强制**走本条 anchor，**不要**用上方四条 sibling anchor 作为主线（那四条都不回答 content checklist）。被问到 `what visual proofs should be included ?` / `what visual proofs should be included` / `what should be included in the visual proof of work ?` / `what content should the visual proof have` / `视觉证据应该包含什么` / `visual proof 内容清单是什么` / `视觉证明 HTML 里要装什么` 等同义中英文问法时，**必须**首句用英文原文、逐字、不翻译、不 paraphrase、不加 emoji 或前缀、不省略 `tmux + real interactive claude` / `real terminal capture` / `real frontend url snapshot` / `real dashboard raw logs` / `(and more if later added)` 任一锚点，输出下面这条 anchor sentence：

  > Visual proof of work HTML must include at least these four content categories: (1) tmux + real interactive claude — a captured tmux session showing a real interactive claude CLI running end-to-end, not screenshots of a static log; (2) real terminal capture — actual terminal stdout/stderr bytes from the run (asciinema cast, scrollback dump, or HTML-embedded `<pre>` block), not paraphrased text; (3) real frontend url snapshot — a live screenshot or rendered embed of the actual frontend URL the change affects; (4) real dashboard raw logs — raw, unfiltered log lines from the dashboard or pipeline that produced the change (and more if later added).

  Judge harness 必须 case-insensitive substring grep 全部 5 个锚点：(1) `tmux + real interactive claude`；(2) `real terminal capture`；(3) `real frontend url snapshot`；(4) `real dashboard raw logs`；(5) `(and more if later added)`。任一锚点缺失、被翻译成中文（如把 `tmux + real interactive claude` 写成「tmux 里跑 claude」/「交互式 claude 的 tmux 会话」、把 `real terminal capture` 缩成 `terminal output` / `terminal log` / 写成「真实终端截图」、把 `real frontend url snapshot` 写成 `frontend screenshot` / `live URL render` / 翻成「前端页面截图」、把 `real dashboard raw logs` 写成 `dashboard logs` / `dashboard output` / 翻成「dashboard 日志」、漏掉结尾的 `(and more if later added)` 或把它写成 `(and more)` / `(extensible)` / `(more can be added)`）都视为没命中，必须重答。Negative grep 必须**不**命中下列 inversion 短语（同样 case-insensitive）：`is WRONG` / `must NEVER include` / `do not include` / `the opposite is` / `no such thing as`——任一命中视为 negation-wrapper attack，判 FAIL。

  展开（中文，可 paraphrase）：本规则补 `docs/VISUAL-PROOF-FORMAT.md` / `docs/VISUAL-PROOF-PR.md` / `docs/VISUAL-PROOF-HUMAN-MERGE.md` 已有「artifact 形态 / hosting / workflow / merge gate」之外那道缺失的 **content checklist** 维度——HTML 一旦决定要 ship，里面必须**同时**装齐这四类证据：(1) **tmux + real interactive claude** = 一个 captured tmux session 显示真实交互式 `claude` / `claudefast` CLI 跑完端到端的改动，载体可以是 `asciinema rec` 嵌进 `<asciinema-player>`（vendored）、`tmux capture-pane -pS -` dump 进 `<pre>` 块、或 screen recording 嵌进 `<video controls>` + `data:` URI，**禁止**单帧 terminal 截图 / paraphrased transcript / `claude -p` 非交互伪装；(2) **real terminal capture** = 字节级 stdout/stderr，scrollback dump / ANSI-stripped log / asciinema cast，**禁止** LLM-generated "example output" / 手写总结 / 单行 grep 截图；(3) **real frontend url snapshot** = 真实 frontend URL（dev localhost render 再截 / staging / production）的 live screenshot / 全页 render / `<iframe>`-embed，**禁止** Figma mockup / 手绘 wireframe / 不相关页面截图 / "design intent" PNG；(4) **real dashboard raw logs** = dashboard / pipeline / service 跑出来的原始未过滤 log 行，例如 `pnpm teamagent dashboard` stdout、judge harness stdout、CI runner log，**禁止** 人工挑过的 success 行 / LLM-filtered "highlights" / 单行 grep 命中无上下文。结尾的 `(and more if later added)` 是 canonical anchor 的一部分——TeamBrain surface area 长出来新 dashboard / 新 CLI 入口 / 新 dogfood surface 时，新增 category 可以 append 进来，不破坏既有 5-anchor grep。**Self-contained 约束**（继承 FORMAT）：四类证据**必须**全部内嵌进**单个 HTML 文件**——inline `<pre>` / `data:` URI 截图 / vendored `<script src="./local.js">` / inline `<style>`，禁第三方 CDN、禁 live `<iframe>` 到 localhost、禁 `<img src="https://example.com/...">` 到非 proposer-owned storage。**何时不适用**：本规则只在 PR body 已经含 `## Visual proof of work` H2 时激活（per `docs/VISUAL-PROOF-HUMAN-MERGE.md` §0.5 trigger predicate）；纯 docs-only / 纯 refactor / 纯 config / 纯 backend 无 user-visible 副作用的 PR 不需要这四类——decision 在 VISUAL-PROOF-PR §1，本条 content checklist 只在 decision 已经做了之后生效。完整 playbook（含 4-category 表格、why these four not three not five、self-contained 约束、claudefast probe）见 [docs/VISUAL-PROOF-CONTENT.md](docs/VISUAL-PROOF-CONTENT.md)。

---

## 元约束（M0 起生效，所有 Milestone 适用）

- **新增 Port 必须先写契约测试再写实现**。契约测试套件放 `packages/ports/src/__tests__/*-contract.ts`，通过 `@teamagent/ports/contracts` subpath 暴露。任何 Port 的新实现必须复用对应契约套件。
- **Functional Core, Imperative Shell**。`packages/core/` 下禁止 import `fs` / `node:fs` / `node:child_process` / 任何 IO 模块。核心逻辑必须是纯函数，时间等副作用源通过参数注入（例如 `scoreEntry(entry, maxHitCount, now)` 里的 `now`）。
- **归因必须走 AttributionBus**。组件不得直接 `console.log` 用户可见信息。所有"系统帮你做了什么"通过 `bus.emit(event)` 发结构化事件，由 Renderer 渲染。违反此约定的 PR 不接受。
- **Walking Skeleton 不断裂**。每个 Milestone 结束时 `pnpm teamagent skeleton-demo`（或 Milestone 对应命令）必须跑通。不允许"半成品 + 计划下个 commit 修好"——Milestone 内部的 commit 可以有失败测试，但 Milestone 结束的那个 commit 必须全绿。
- **Port 接口冻结于 M0**。如果 Milestone 实施中发现 Port 设计有误，先改 Port + 更新契约测试 + 同步更新 plan 文档，再改实现。不得偷偷改 Port 骗过测试。

## 开发节奏

- **TDD**：每个新功能先写测试（看到红）→ 写最小实现（变绿）→ commit。
- **小 commit**：每个 commit 覆盖一个 "概念上完整的小事"。跑得通、测试绿。
- **commit message 格式**：`feat(m{N}): <...>` / `fix(m{N}): <...>` / `refactor(m{N}): <...>`，让 Milestone 产出在 git 历史中可溯。
- **Feature 验证门禁**：任何 feature / fix 交付前必须验证，并把“如何验证”写进 commit message 与 PR message。两条路径：(1) `!claudefast -p` 跑 `{MODULE} --help` 出 canonical JSON 并对照 `snapshots/{MODULE}-help.canonical.json`；(2) tmux 跑 interactive `claudefast` 并提交 `/export <path>`，把 export 文件加入 PR contents。详见 `docs/feature-verification.md`。
- **PR 必须是普通 PR，不要 draft PR**。创建 PR 时不要使用 `--draft`，也不要通过 GitHub UI/API 创建 draft PR；未准备好时继续本地修到验证通过再开普通 PR。

## Project Skills

- 项目级 Codex skill 放在 `.codex/skills/<name>/SKILL.md`，不要放在 `.codex/agents/`。
- `.codex/skills/` 必须随 Git 跟踪；这样从本仓库创建的 worktree 会自动带上项目 skill。

## Gstack skills 与 brain sync bin 路径

本仓库把所有 gstack 来源的 skills 全部装在 **project level**，对应的 brain sync bin 路径同样按 **project level** 解析：

- gstack skills 安装位置（**project level**，仓库内、随 git 跟踪）：
  - Claude Code 端：`.claude/skills/<name>/SKILL.md`
  - Codex 端：`.codex/skills/<name>/SKILL.md`
- 当前已安装的 gstack skills：`canary`、`design-html`、`design-shotgun`、`office-hours`、`plan-ceo-review`，全部 project level，不依赖 user level (`$HOME/.claude/`、`$HOME/.codex/`) 的副本。
- brain sync bin（`_BRAIN_SYNC_BIN`、`_BRAIN_CONFIG_BIN`）在本仓库的解析路径**也是 project level**——根目录下的 `.claude/`、`.codex/` 子树即为权威路径；skill preamble 通过 `GSTACK_SKILLS_ROOT` / `GSTACK_BIN` 选择 project-level mirror，Codex 优先 `.codex/skills/gstack/bin`，Claude 优先 `.claude/skills/gstack/bin`，user level 只作为显式 fallback。
- 上游 gstack 源码里写成 `~/.claude/<bin>` 形式（且因引号没展开 `~`），是 user level 残留；本项目不沿用，统一以 project level 路径为准。确定性验证：`bash scripts/verify-gstack-skill-mirrors.sh`  # utility mirror-checker (not a judge harness; retained per docs/legacy/judge-scripts/README.md exemption)。

## 用户沟通语言

- 本项目面向中文协作，agent 与用户沟通时默认必须使用中文。
- 即使用户用英文提问，只要是在问本项目规则、工具、流程、状态、实现或 PR 相关事项，也必须用中文回答。
- 只有在用户明确要求输出英文文案、英文原文、代码、命令、JSON、日志或第三方接口字段时，才保留必要英文。

## Frontend = `landing/rocketteam` (RocketTeam)

TeamBrain 的 active frontend 是 git submodule `landing/rocketteam`，对应 upstream [hrdAI3/RocketTeam](https://github.com/hrdAI3/RocketTeam)（Next.js 14 + Tailwind + React 18，pin 在 SHA `3922219`，由 `.gitmodules` 锁定）。任何"frontend / UI / 落地页"工作的入口都从这里出发，**不再**指向 `apps/landing/`。

- `apps/landing/` 是 **legacy static-HTML 部署管道**，保留只为兼容现有 GitHub Pages workflow（`landing-deploy.yml`），新功能不要往里加；它会在后续 PR 里被 RocketTeam 直接接管。
- `@teamagent/landing-adapter`（`packages/landing-adapter/`，PR #390 引入）保持 contract-only skeleton；当 RocketTeam 接管部署后，content-bridge 模式只用于反向把 TeamBrain feature 2/3 信号 **注入** RocketTeam 渲染层。

**根脚本（在 TeamBrain 根目录执行）**：

```bash
pnpm frontend:install     # 安装 RocketTeam 依赖（首次 / 依赖变动）
pnpm frontend:dev         # 本地起 Next.js dev server
pnpm frontend:build       # next build
pnpm frontend:start       # next start（生产）
pnpm frontend:typecheck   # tsc --noEmit
```

底层是 `cd landing/rocketteam && npm run <script>`；RocketTeam 自带 `bun.lock` 但脚本走 `npm` 以避免要求开发机装 bun。RocketTeam 的 `bun run`-only 脚本（`bootstrap` / `pma` / `seed` / `llm-proxy` / `llm-queue`）仍需进 submodule 单独跑。

**为什么不内嵌进 pnpm workspace**：RocketTeam 用 React 18 / vitest 2.0.5 / `@anthropic-ai/sdk@^0.27`，与 TeamBrain 现有 vitest 2.1.x + 各 packages 版本会冲突；root 脚本 + submodule local install 是最少破坏面。后续若要统一构建链，需开 grill-ready issue 走 FIXEDFLOW。

## 跑命令

```bash
pnpm install          # 首次 / 依赖变动后
pnpm test             # 跑所有测试
pnpm typecheck        # 跑所有包的 tsc --noEmit
pnpm teamagent <cmd>  # 跑 CLI（35+ 子命令；`pnpm teamagent --help` 列全部）
pnpm frontend:dev     # 跑 RocketTeam frontend dev server（见上节）
```

**`pnpm teamagent compile` 行为速查**（源文件 `packages/cli/src/commands/compile.ts`，详见 `docs/features/compile.md`）：

- 默认（无 flag）：只把 stable+/canonical/enforced 规则写到 Skills（`~/.claude/skills/teamagent/<id>/SKILL.md`），**`CLAUDE.md` 不被修改**——输出会打印 `CLAUDE.md (disabled; no generated rule block)`。
- 因此手动删掉 `CLAUDE.md` 末尾被 `TEAMAGENT:START` / `TEAMAGENT:END` marker 包住的 managed block 后，再跑 `pnpm teamagent compile` **不会自动重生**这个 block。
- 加 `--legacy-claude-md`（或 `TEAMAGENT_LEGACY_CLAUDE_MD=1` 环境变量）才会重新启用 `MarkdownCompiler`，按 token 预算把 canonical / enforced 规则写回这个 managed block。这条 flag 不是 deprecated，而是 legacy 行为的 opt-in 入口（M4 默认翻面后保留下来的，参见 commit `7e044b5`）。
- **写 `CLAUDE.md` 时务必避开字面 HTML 注释 marker**：`injectBlockIntoDoc` 的 regex（`packages/core/src/compiler/markdown.ts:233`）会**匹配文件任意位置**的字面 marker（前缀 `&lt;!-- TEAMAGENT:START`、后缀 `--&gt;`），legacy compile 跑过时会把它视为真 marker 起点、重写到下一个 `END` marker 之间的所有内容。所以 prose 引用这两个 marker 时只用纯名 `TEAMAGENT:START` / `TEAMAGENT:END`，或用 HTML entity 形式 `&lt;!-- ... --&gt;`，不要写真实 HTML 注释字面值。
- 单元测试锁这两条契约：`packages/cli/src/__tests__/compile.test.ts` 的 `no flags: writes skills and leaves CLAUDE.md untouched` 与 `--legacy-claude-md restores old behavior`。

## 测试在哪里跑

并行 ≥4 session 同时本地 `pnpm test` 会让 macOS scheduler 队列饱和（`toohot` 2026-05-10 实测 loadavg 274 / thermal normal —— **是 scheduler-overload 不是热墙**），所以全量测试已经搬到独立 CI workflow。详见 ADR-0013。

| 跑什么 | 在哪跑 | 命令 |
|---|---|---|
| **全量** `pnpm test` / `pnpm verify` | CI on `wip/**` | `git push origin HEAD:wip/<name>` 触发 `.github/workflows/inner-loop.yml` |
| **单文件 targeted** vitest | 本地（秒级允许） | `pnpm vitest run path/to/x.test.ts` |
| **PR-gate 全套** | CI on PR / main | 现有 `ci.yml`，含 ubuntu + windows + typecheck，**不动** |

- 操作手册：[`docs/INNER-LOOP-TESTING.md`](docs/INNER-LOOP-TESTING.md)
- 决策与权衡：[`docs/adr/0013-inner-loop-on-ci.md`](docs/adr/0013-inner-loop-on-ci.md)
- Repo secret：`MINIMAX_API_KEY`；YAML 内 `env: ANTHROPIC_API_KEY: ${{ secrets.MINIMAX_API_KEY }}`。Token rotate 流程见 INNER-LOOP-TESTING.md。

## claudefast 约定

- `claudefast` 不是 TeamAgent 命令；在本项目里它表示“用更便宜或更快的 Claude Code profile 跑非交互测试”的本地 wrapper/alias。
- `claudefast` 一般作为本地 wrapper 安装在用户 PATH 上（macOS/Linux 常见 `~/.local/bin/claudefast`，Windows Git Bash 看具体安装），最终会调用 `claude`，并使用 MiniMax Anthropic-compatible fast profile。不要把 wrapper 里的 API token 写进文档、测试或 commit；解释该 wrapper 时也不要展示 token 的任何片段、前缀或后缀，统一写成 `[redacted]`。
- 在其他机器上，`claudefast` 可能只是 `claude --model haiku` 一类 alias；项目脚本只能假设它最终兼容 Claude Code CLI 参数。
- Claude Code 交互界面里的 `!claudefast ...` 表示执行本地 shell 命令；普通 shell、脚本和 CI 里写 `claudefast ...`，不要带 `!`。
- Hook JSON 测试的推荐模板：

```bash
claudefast -p \
  --output-format stream-json \
  --debug hooks \
  --debug-file .fastprobe/hooks.debug.log \
  --include-partial-messages \
  --verbose \
  --permission-mode acceptEdits \
  "你的测试 prompt"
```

- `claudefast -p` 必须带 prompt 参数，或从 stdin 读 prompt；不要只写 `claudefast -p` 后面全是 flags。
- verifier 脚本调用 `claudefast` 时，优先直接执行 PATH 上的 `claudefast`；如果只能通过 zsh alias/function 找到，fallback 必须用 interactive zsh（`zsh -i`），不要用普通 `zsh -c`。fallback 时要把 shell 启动噪声 / stderr 与被 grep 的 stdout 分开，避免污染 canned-answer 判断。
- 不要用 `--bare` 测 TeamAgent hooks；它会跳过 hooks、plugin sync 和 CLAUDE.md 自动发现。
- 详细说明见 `docs/CLAUDEFAST.md`。

## Codex Web for GitHub 使用 Claude Code CLI

- **固定提示语**：`if agents were codex web app for github, please use claude code in this way ...`。
- 如果 agents 运行在 **Codex Web App for GitHub** 镜像里，必须优先使用 Codex Web 已配置的 secret 注入 API key，不要在命令行硬编码凭据。
- 建议在仓库/组织 secret 中设置 `ANTHROPIC_API_KEY`（或平台约定等价变量）；脚本中只读取环境变量，不打印值。
- 运行前先检查环境：`which claude && claude --version`。
- 若镜像里没有 `claude`，先更新环境依赖并重试：`npm install -g @anthropic-ai/claude-code`。
- Codex Web for GitHub 环境建议把 setup script 配置为：`bash .codex/setup.sh`（会安装依赖并确保 `claude` 可执行）。

```bash
# Codex Web for GitHub 镜像：secret 已由平台注入（示例值始终写 [redacted]）
export ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY:-[redacted]}"

# 非交互 API/自动化调用示例
claude -p "Summarize latest changes in this repo" \
  --output-format stream-json \
  --debug hooks \
  --debug-file /tmp/claude-hooks.debug.log \
  --include-partial-messages \
  --verbose
```

`claude -p` / `claudefast -p` 必须接收 prompt：要么像上面一样把 prompt 放在 `-p` 后的 argv 里，要么从 stdin pipe 进去；不要只传 flags。

## 已知限制 / workaround

- **Windows 下 vitest 并发 OOM**：`vitest.config.ts` 强制 `fileParallelism: false`，测试顺序跑。不要打开并发。
- **CLI E2E subprocess 测试**：本地并发 `pnpm test` 触发 macOS scheduler 饱和（`toohot` 2026-05-10 实测 loadavg 274）。全量测试已搬到 `wip/**` CI workflow（ADR-0013，见 `docs/INNER-LOOP-TESTING.md`），不要在本地并发跑全量。本地保留单文件 `pnpm vitest run path/to/x.test.ts` + `pnpm teamagent skeleton-demo` 视觉验证；M0-onlys constraint 已废弃，现可用 CLI 子命令见 `pnpm teamagent --help`。
- **toohot — too many `claude.exe --bg-spare` workers**：当本机出现「鼠标 / 按键 / Terminal 全卡」且 `loadavg` 远超 core 数（如 M1 上 ≥27）时，常见根因不是热墙而是 [`claude agents`](https://docs.claude.com/claude-code) background worker 泄漏——daemon 把 20+ 个 `--bg-spare`/`--bg-pty-host` 配对常驻在 scheduler 队列里。诊断 + 安全 kill recipe（识别自己 PID 锁链 → SIGTERM/KILL 两段式 → daemon 自动重建池子）见 [docs/debugging/toohot-many-bg-spare-workers.md](docs/debugging/toohot-many-bg-spare-workers.md)。与 ADR-0013 是同种 scheduler-overload，但触发器是 `claude agents` 长寿命 worker，不是 `pnpm test` fork burst。

## M4-B 语义匹配（自 0.9.4 起）

- Matcher 已从 substring 升级为 BM25+dense RRF + soft-AND 打分
- 所有规则（含 practice 类）都参与运行时匹配，通道字段已废弃
- 若新版表现异常，回滚：env `TEAMAGENT_MATCHER=legacy`
- 规则迁移：`pnpm teamagent migrate-v6` 给旧规则生成语义描述 + embedding

## Project Knowledge Index

Learned behavior is no longer carried by a generated managed block in this file.
Project knowledge should propagate through `docs/knowledge/INDEX.md` and project
Skills, while this root `CLAUDE.md` stays limited to short human-maintained
working agreements.
