```text
   ┌──────────────────────────────────────────────────────────────────┐
   │  PR #254 — issue-244 update-state lock — FIX PLAN (iter-by-iter) │
   │                                                                  │
   │   issue #244 ──► PR #254  ──► /review loop ──► squash-merge      │
   │                       ▲                                          │
   │                       └── iter-1 finding (this file)             │
   └──────────────────────────────────────────────────────────────────┘
```

# PR #254 fix plan — `/review` 循环修订记录

承接 `docs/PR-PLAN.md`：PR 已开后再发现的 issue **不开 follow-up issue**，写在本文件，原 PR branch 修。

## Context (linkage)

- **Issue**: #244 — `update-state.json` read-modify-write race
- **PR**: #254 — `[issue-244] update-state.json read-modify-write file lock`
- **Branch**: `feat/issue-244`
- **FIXEDFLOW state**: step 4 `/review` loop（无第三方 reviewer，driver + 本地 `/review` skill）
- **Worktree**: `.codex/worktrees/issue-244`

---

## ① iter-1 — pre-landing review pass

### Task description

跑本地 `/review` skill 在 `git diff origin/main` 上做 critical + informational pass，对每条 finding 按 Fix-First Heuristic 决定 AUTO-FIX 还是 ASK。

### Findings

| # | Severity | File:Line | 问题 | 处理 |
|---|----------|-----------|------|------|
| 1 | INFORMATIONAL | `packages/cli/src/bin-updater.ts:108-119` | field-ownership docstring 列了 11 个 updater-owned 字段 + 4 个 foreground-owned 字段，但漏了 `reinstall_banner_shown_at`（session-start-logic `maybeShowReinstallBanner` 自己持，session-start 写 → 自动经 `withUpdateStateLock` 透传）。merge 实现正确（`...live` spread 保住），但未来 reader 若把它加进 overlay 会无声 clobber reinstall banner 冷却 | **AUTO-FIX**：docstring 加第三段 `session-start owns: reinstall_banner_shown_at`，commit `e037e2b` |

### Expected outputs

- ✅ docstring 增 3 行，明确 `reinstall_banner_shown_at` 由 session-start-logic 持有；
- ✅ 无行为变更；
- ✅ typecheck pass；
- ✅ atomic commit `docs(issue-244): /review iter-1 — document reinstall_banner_shown_at ownership` (e037e2b)。

### How-to-verify (judge harness)

```text
RUN  : pnpm typecheck && grep "session-start owns" packages/cli/src/bin-updater.ts
DUMP : exit 0 + grep prints the new line
READ : LLM judge 仅读 raw grep stdout 判断 docstring 是否包含 "session-start owns: reinstall_banner_shown_at"
```

---

## ② iter-2 — final pass (target: PASS)

### Task description

`git diff origin/main` 在 iter-1 fix commit 之后再跑一遍 `/review`，确认 critical + informational 两 pass 都无新 finding。

### Expected outputs

- 无 finding；
- `gstack-review-log` 写 `status:"clean"`；
- driver 进入 step 5 squash-merge。

### How-to-verify (judge harness)

```text
RUN  : 跑 /review skill 内嵌的两 pass + 4 个回归 test suite + 新单测；
DUMP : judge.json {iter:2, critical:0, informational:0, regression:0, lock_tests:10};
READ : LLM judge 只读 raw JSON + final commit SHA，判断 0 finding 才能 PASS。
```

---

## Iter counter

`./.fixedflow/iter-244.json`：

```json
{ "issue": 244, "iter": 2, "pr": 254, "started_at": "2026-05-10T00:18:00Z", "last_iter_at": "2026-05-10T00:21:00Z", "tokens_cumulative": 0 }
```

如果 iter-2 找到新 finding，本文件**追加 ③ iter-3 段**，**禁止开 follow-up issue / PR**。
