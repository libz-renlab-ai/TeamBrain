```
   PR #200 fix loop — iter 1 (manual /review pass)

   findings ──→ write this plan ──→ fix in same branch ──→ commit ──→ re-verify
```

# PR-PLAN: PR #200 fix iter 1

> Per `docs/PR-PLAN.md`: when /review surfaces issues post-PR, do NOT open a
> follow-up issue; write a per-iter fix plan and apply in the same PR branch.

## §1 task description

Manual /review pass on PR #200 found 2 actionable items in
`scripts/fixed-flow-watcher.sh`:

- **P1**: watcher uses `jq` and `gh` without verifying availability;
  missing tool causes silent error cascade
- **P2**: `write_heartbeat()` writes directly with `> file` (not atomic);
  signal/process kill mid-write leaves a half-written JSON file the
  cloud heartbeat Action would fail to parse

Other categories scanned (shell injection, race in mark_processed, CJK regex
range, GHA numeric coercion, bypass-permission-check, cron schedule, driver
skill description) — all pass.

## §2 expected outputs

- watcher fails fast at startup with `[FATAL] required tool not in PATH: <tool>`
  and exit 127 if `jq` or `gh` missing
- `write_heartbeat()` uses `mktemp + mv` atomic-rename pattern (same shape as
  existing `mark_processed()`)
- `bash -n scripts/fixed-flow-watcher.sh` syntax check passes
- `bash scripts/fixed-flow-watcher.sh --dry-run --issue 999` still prints
  `[DRY] would invoke mainpi for issue 999` (judge.md anchor 6 unchanged)

## §3 judge harness (md playbook, NOT fixed bash)

Add to existing `docs/plans/2026-05-09-fixed-flow/judge.md` § notes:
- §V1 RUN add probe F: `command -v jq && command -v gh && bash scripts/fixed-flow-watcher.sh --dry-run --issue 999`
- §V2 DUMP add metric `dependency_check_present: true`
- §V3 READ add anchor 8: dry-run output contains exactly the expected line
  AND the script source contains a `command -v jq` startup gate

For this iter, manual verification only: confirm bash -n + dry-run pass post-fix.
