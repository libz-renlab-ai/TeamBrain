```
 ___  _         ___  ___
| _ \/ |  _    | _ \|_  )
|  _/| | (_)   |  _/ / /
|_|  |_| (_)   |_|  /___|

P1 现状盘点 ──► 已知决策 / 功能矩阵 / 开放 gaps
P2 install 对比 ──► 4 选项 × 4 维度 → 推荐
```

# Probes P1 + P2 — Research Foundations

Worker: worker-1 (sonnet via claudefast / MiniMax-M2.7-highspeed)
Branch: worktree-issue84

---

## P1 — 现状盘点

- Probe intent: 读 spec + features + plan，找出 already-decided / features-to-surface / 仍 open 的 gaps
- Raw stream: `.fastprobe/issue84/p1.stream.json`
- Extracted JSON: `.fastprobe/issue84/p1.json` (valid)
- Debug log: `.fastprobe/issue84/p1.debug.log`
- Findings:
  - 7 条 grill 决策全部 closed：定位外部访客、B+C+GIF 组合、Hero 对比表、双瞬间 GIF、两阶段 install、pack 委托 agent、`<details>` 折叠二屏
  - Hero H1 在 spec 内已有定稿：pain-first "Claude Code 没有记忆" 句式，不再是待定项（但 P1 probe 仍标为 gap，说明 probe 读到草图而非定稿 leaf；实际 leaf 1 已 RESOLVED）
  - 8 个 features 已精确定位：#3/#4 在 Hero+GIF，#10 在工作原理图，#21-25/#31-33/#48 在 `<details>`，#38-40 在 trust anchor，#44 在 footer
  - 10 个 gaps 均为实现侧（N1–N6 新 feature + apps/landing/ + Pages workflow + TTHW 验证）——无设计决策遗留
- Recommendation: spec 决策层干净，研究重心可转向 N1–N6 优先级排序与 Pages 部署拓扑

---

## P2 — install one-liner 对比

- Probe intent: 4 选项 × 4 维度 0-10 评分，给出推荐 + 次选
- Raw stream: `.fastprobe/issue84/p2.stream.json`
- Extracted JSON: `.fastprobe/issue84/p2.json` (valid)
- Debug log: `.fastprobe/issue84/p2.debug.log`
- Findings:
  - 推荐: D Claude Code plugin marketplace（friendliness=9, security=8, audit_cost=3, rollback=9）——三维最优
  - 次选: A npm install -g（friendliness=8, security=6, audit_cost=7, rollback=5）——传统 CLI 用户友好
  - C curl|sh 最差（security=2, audit_cost=10）——但这正是 spec 决策 5 选定的方案；probe 无关现实约束
  - 关键 trade-off: spec 已锁定 curl|sh（决策 5，两阶段 install），P2 评分显示其安全性与审核成本代价最高；需要在 install.sh 加 TLS + checksum + dry-run 机制对冲（P4 安全审计 probe 接力）
- Recommendation: spec 选 C 是合理的"开发者信任开源社区"路线；建议 install.sh 加 SHA256 checksum + `--dry-run` flag 降低顾虑

---

## Feeds research.md §D（reporter 回填用）

- Row P1: status=done, 结论=7 条决策 closed; 8 features 位置确认; 10 gaps 均为实现侧 N1-N6 + Pages + TTHW
- Row P2: status=done, 结论=推荐 D(marketplace); 次选 A(npm -g); spec 锁定 C 需安全加固(交 P4)
