---
Issue: "#310"
Title: '2. 录音要方便'
State: OPEN
Closed-at: —
Closing-PRs: —
Saved-at: 2026-05-13
Saved-by: /grill-with-docs save-mode (batch grill from ChatGPT share)
Source-URL: https://github.com/libz-renlab-ai/TeamBrain/issues/310
Grill-URL: https://chatgpt.com/s/t_6a03861d49b081918ed2c900f3870c46
---

# Grill log: issue #310

> Saved per ADR-0014 §"Operational shape" step 3 from a unified 33-section
> batch grill covering 12 TeamBrain issues. Source: [chatgpt.com share](https://chatgpt.com/s/t_6a03861d49b081918ed2c900f3870c46).
> Cross-cutting overview (§0/§1/§24/§30/§31/§32) + architecture (§3/§5/§9/§16/§17) +
> feature specs (§25–§29) live in the three `batch-2026-05-13-*.md` siblings. Below
> are the §s where #310 is named or listed as a touching issue.

## Issue metadata

- **Title**: 2. 录音要方便
- **State**: OPEN
- **Labels at save time (before P3 → P4 flip)**: docs-grill-ready, grill-ready
- **Labels expected post-flip**: docs-grill-ready, grill-ready
- **Source URL**: https://github.com/libz-renlab-ai/TeamBrain/issues/310
- **Unified grill source**: https://chatgpt.com/s/t_6a03861d49b081918ed2c900f3870c46

## §31 verdict row for #310

> PWA 一键录音 + CLI 共用 Audio Event API

## Grill sections that touch #310 (verbatim)

# 19. audio 线：#296/#297/#310 必须统一成 `audio` event source

## Grill 问题

录音是个人记忆、项目证据、日报输入，还是异常输入？

## 可选答案

A. 个人记忆
B. 项目证据
C. 日报输入
D. 异常输入
E. audio 是 event source，用途由 plugin 决定

## 裁决

选 **E**。

```txt
AudioRecordingStarted
AudioRecordingStopped
AudioUploaded
AudioTranscriptReady
AudioSummaryReady
```

plugins 决定用途：

```txt
RecordingMemoryPlugin:
  进入 personal/team memory

ProjectEvidencePlugin:
  挂到 project/issue/PR

DailySummaryPlugin:
  可选进入日报

AbnormalDetectorPlugin:
  可选扫描 blocker/risk/stuck
```

#296 说明两个独立子系统都用了 “recording”：一个是 `teamagent recording import|search|show|inject|metrics|benchmark` 的 Recording Memory，另一个是 `teamagent record start|stop|import` 的 digital-twin audio recorder，而且后者还上传到 `/v1/recordings`；issue 明确指出这会让用户混淆。citeturn148789view0turn148789view1

所以命名裁决：

```txt
新命名：
  teamagent audio start
  teamagent audio stop
  teamagent audio import
  teamagent audio devices

API:
  /v1/audio-recordings
  /v1/events

事件：
  AudioRecordingStarted
  AudioTranscriptReady

兼容：
  teamagent record start -> alias to teamagent audio start
  /v1/recordings -> deprecated alias
```

---

# 21. #310：手机 Web/PWA 一键录音，而不是只修 CLI

## Grill 问题

“不需要开电脑，随时开录”怎么实现？

## 可选答案

A. 只做 CLI
B. 只做移动 app
C. CLI + 手机 Web/PWA 共用 Audio Event API
D. 做 Slack/飞书 bot 上传
E. 先只改文档

## 裁决

选 **C**。

#310 的需求只有一句：“不需要开电脑，随时开录”。这意味着 CLI-only 不够。citeturn138076view6

架构：

```txt
Desktop:
  teamagent audio start/stop/import

Mobile:
  PWA 一键录音
  background chunk upload
  offline local cache
  retry/resume

Backend:
  Audio Event API
  transcription job
  summary job
  plugin dispatch
```

手机录音归属：

```txt
从项目页录音：
  直接挂 project

从全局快捷入口录音：
  先进 personal audio inbox

AI 自动归类：
  只建议 project/issue/PR
  不自动强绑定
```

避免 AI 错归类污染 #371 日报和 #372 异常。

---

--- end grill ---

<!-- end grill log -->
