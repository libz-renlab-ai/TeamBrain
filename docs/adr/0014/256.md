---
Issue: "#256"
Title: [fixedflow] 清理 PR #255 排除的 5 处 superpowers/sales 残留字面值 
State: CLOSED
Closed-at: 2026-05-09T17:16:38Z
Closing-PRs: #257
Saved-at: 2026-05-12
Saved-by: maintainer backfill (post-ADR-0014 acceptance)
Source-URL: https://github.com/libz-renlab-ai/TeamBrain/issues/256
---

# Grill log: issue #256

> Saved verbatim from GitHub comments per ADR-0014 §"Operational shape" step 3.
> Save-mode backfill: issue pre-existed before ADR-0014 was accepted (2026-05-11),
> so this file is written from existing GitHub grill comments — no new grilling.

## Issue metadata

- **Title**: [fixedflow] 清理 PR #255 排除的 5 处 superpowers/sales 残留字面值
- **State**: CLOSED
- **Closed at**: 2026-05-09T17:16:38Z
- **Closing / related PRs**: #257
- **Source URL**: https://github.com/libz-renlab-ai/TeamBrain/issues/256

## Grill comments (verbatim)

### Comment 1 — @LiuShiyuMath at 2026-05-09T16:55:49Z

# Grill — 清理 superpowers/sales 残留字面值（PR #255 follow-up）

按 `docs/HOWTO-PLAN-PR.md` 4 段结构产出。如有歧义先在 `docs/plans/2026-05-10-issue-256/research.md` 记录再请示，不得自决偏离。

## 1. Plan / 任务描述

PR #255 grill 明文排除了 5 处 `superpowers` / `sales` / `caveman` 残留字面值，留作正交清理。本 PR 把这 5 处全部清掉，让 `claudefast -p "in THIS project, what files we have related to superpowers ?"` probe 的输出**不再**把以下文件列为 superpowers-related：

| 文件 | 残留位置 | 现状字符串 → 替换为 |
|---|---|---|
| `packages/cli/src/bin.ts` | L480 help text | `superpowers/caveman/sales` → `code-review/code-simplifier/frontend-design` |
| `packages/cli/src/bin.ts` | L1230 help text | `superpowers/sales/playground` → `playground/code-review/frontend-design` |
| `packages/cli/src/commands/init.ts` | L91 docstring | `superpowers/sales/playground` → `playground/code-review/code-simplifier` |
| `packages/cli/src/commands/init.ts` | L1546 banner | `superpowers/sales/playground` → `playground/code-review/code-simplifier` |
| `packages/teamagent/CLAUDE.md` | L20 + L21 自动 block 旧 learnings | 整行删除（default `compile` 不重写 CLAUDE.md，`--legacy-claude-md` 才会） |
| `scripts/evidence-phase-gaps-ab.ts` | L224 `targetPlugins = ["superpowers", "sales", "playground"]` | 改为 `["playground", "code-review", "frontend-design"]`；L219 / L256 `docs/superpowers/specs/...` 是真实历史路径，**保留**（不是 plugin 残留） |
| `packages/core/src/m5/__tests__/bootstrap-diff.test.ts` | L8 / L17 / L60 fixtures | `["superpowers", "caveman"]` → `["playground", "code-review"]`（占位名，测试不依赖字面值） |
| `packages/core/src/m5/__tests__/manifest.test.ts` | L12 fixture | `["superpowers"]` → `["playground"]` |
| `packages/cli/src/__tests__/m5-cli.test.ts` | L97 / L101 fixture | `["caveman", "superpowers"]` → `["code-review", "playground"]` |
| `packages/adapters/src/m5/__tests__/fs-bootstrap.test.ts` | L15 fixture | `["superpowers"]` → `["playground"]` |
| `packages/cli/src/__tests__/install-plugins.test.ts` | L45-46 CSV parser 测试字面 `--only=superpowers,sales` | 改 `--only=playground,code-review`；parser 不验 bundle，等价 |
| `packages/cli/src/__tests__/sandbox-all-features.test.ts` | L432-435 同 CSV parser 测试 | 同上替换 |
| `packages/adapters/src/plugins/__tests__/claude-plugin-installer.test.ts` | L133-209 fixtures `superpowers@claude-plugins-official` / `sales@knowledge-work-plugins` / `ghost@knowledge-work-plugins` | 全部 swap 为 current bundle 名 + marketplace `claude-plugins-official`（保留 `ghost@<不存在的 marketplace>` 失败用例语义，但 `<不存在>` 也换成 `nonexistent-marketplace` 避免 stale ref） |

**保留不动**（不是残留）：
- `docs/superpowers/specs/...`、`docs/superpowers/plans/...` — 这是真实 milestone 目录名（Phase 2+）
- `docs/CONTEXT.md` / `docs/FASTPROBE.md` / `docs/README.md` / `docs/SELF-UPDATE.md` 里指向 `docs/superpowers/` 的 link
- `packages/types/src/m5.ts:3` / `packages/core/src/validator/l0.ts:44` 里指向 `docs/superpowers/specs/...` 的 doc link 注释
- `.claude/skills/office-hours/SKILL.md` / `.codex/skills/office-hours/SKILL.md` 里 `"sales skills"` —— 这是英文短语（销售技巧），不是 plugin 残留

## 2. Expected outputs

| 类别 | 路径 | 备注 |
|---|---|---|
| 修改 | `packages/cli/src/bin.ts` | 2 行 help text |
| 修改 | `packages/cli/src/commands/init.ts` | 1 docstring + 1 banner |
| 修改 | `packages/teamagent/CLAUDE.md` | 删 2 行（auto block 内，default 不重写） |
| 修改 | `scripts/evidence-phase-gaps-ab.ts` | targetPlugins 数组（保留 docs/superpowers path 字符串） |
| 修改 | 5 个 m5/install-plugins/sandbox/installer test 文件 | 占位字符串 swap |
| 新文件 | `docs/plans/2026-05-10-issue-256/research.md` | Boris workflow（≤80 行） |
| 新文件 | `docs/plans/2026-05-10-issue-256/judge.md` | V1/V2/V3 三段（≤120 行） |

## 3. How-to-verify / 第三方 judge harness

V1 RUN — fixed tools:

```bash
WT=/Users/m1/projects/TeamBrain/.codex/worktrees/issue-256
RUN_DIR=$WT/.judge/2026-05-10-issue-256
mkdir -p "$RUN_DIR/raw"; cd "$WT"

# 1. 5 处 user-facing surface 不再含 plugin 残留字面值
for f in packages/cli/src/bin.ts packages/cli/src/commands/init.ts packages/teamagent/CLAUDE.md scripts/evidence-phase-gaps-ab.ts; do
  echo "=== $f ===" >> "$RUN_DIR/raw/01-surface.out"
  grep -nE "(superpowers|caveman|sales)" "$f" >> "$RUN_DIR/raw/01-surface.out" 2>&1 || echo "(none)" >> "$RUN_DIR/raw/01-surface.out"
done; echo $? > "$RUN_DIR/raw/01-surface.exit"

# 2. m5 + plugin tests fixtures
for f in packages/core/src/m5/__tests__/bootstrap-diff.test.ts packages/core/src/m5/__tests__/manifest.test.ts packages/cli/src/__tests__/m5-cli.test.ts packages/adapters/src/m5/__tests__/fs-bootstrap.test.ts packages/cli/src/__tests__/install-plugins.test.ts packages/cli/src/__tests__/sandbox-all-features.test.ts packages/adapters/src/plugins/__tests__/claude-plugin-installer.test.ts; do
  echo "=== $f ===" >> "$RUN_DIR/raw/02-fixtures.out"
  grep -nE "(superpowers|caveman|sales)" "$f" >> "$RUN_DIR/raw/02-fixtures.out" 2>&1 || echo "(none)" >> "$RUN_DIR/raw/02-fixtures.out"
done; echo $? > "$RUN_DIR/raw/02-fixtures.exit"

# 3. tests + typecheck still green
pnpm exec vitest run packages/core/src/m5 packages/cli/src/__tests__/install-plugins.test.ts packages/cli/src/__tests__/m5-cli.test.ts packages/cli/src/__tests__/sandbox-all-features.test.ts packages/adapters/src/m5 packages/adapters/src/plugins > "$RUN_DIR/raw/03-vitest.out" 2> "$RUN_DIR/raw/03-vitest.err"; echo $? > "$RUN_DIR/raw/03-vitest.exit"
pnpm typecheck > "$RUN_DIR/raw/04-typecheck.out" 2> "$RUN_DIR/raw/04-typecheck.err"; echo $? > "$RUN_DIR/raw/04-typecheck.exit"

# 4. claudefast probe — must not surface above 5 files
claudefast -p "in THIS project, what files we have related to superpowers ?" > "$RUN_DIR/raw/05-probe.out" 2> "$RUN_DIR/raw/05-probe.err"; echo $? > "$RUN_DIR/raw/05-probe.exit"
```

V2 DUMP — `judge.json`:

```json
{
  "issue": "<N>",
  "exit_code": "<max of step exits>",
  "metrics": {
    "user_facing_surface_clean": "<bool, step 1 stdout has 0 superpowers/caveman/sales hits in bin.ts/init.ts/teamagent CLAUDE.md/evidence script (excluding docs/superpowers/ legitimate paths)>",
    "fixtures_clean": "<bool, step 2 stdout has 0 hits in 7 fixture files>",
    "vitest_passed": "<bool, step 3 exit==0>",
    "typecheck_passed": "<bool, step 4 exit==0>",
    "probe_no_residual_files": "<bool, step 5 stdout does NOT mention bin.ts / init.ts / packages/teamagent/CLAUDE.md / m5 fixtures / evidence-phase-gaps-ab.ts as 'related to superpowers'; legitimate docs/superpowers/* mention IS allowed>"
  },
  "evidence_dir": ".judge/2026-05-10-issue-256/raw/"
}
```

V3 READ — LLM judge gate:

```bash
claudefast -p "Read .judge/2026-05-10-issue-256/judge.json + raw/. Return PASS/FAIL. PASS only if all 5 metrics true. The probe metric specifically requires the probe output (raw/05-probe.out) does NOT list bin.ts / init.ts / packages/teamagent/CLAUDE.md / m5 fixtures / evidence-phase-gaps-ab.ts as 'related to superpowers'. The probe IS allowed to mention docs/superpowers/ (real folder) as related."
```

## 4. claudefast probes (run after merge)

```bash
claudefast -p "in THIS project, what files we have related to superpowers ?"
# Expected: returns docs/superpowers/* (legitimate folder) only; does NOT return bin.ts / init.ts / packages/teamagent/CLAUDE.md / m5 fixtures / evidence-phase-gaps-ab.ts as related to plugin "superpowers".

claudefast -p "in THIS project, what files we have related to plugin sales ?"
# Expected: empty / "none". sales is not a plugin in this project.
```

--- end grill ---


---


<!-- end grill log -->
