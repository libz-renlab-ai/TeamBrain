---
Issue: "#218"
Title: [fixedflow] init 输出无 FIXEDFLOW 引导 
State: CLOSED
Closed-at: 2026-05-09T11:29:48Z
Closing-PRs: #226
Saved-at: 2026-05-12
Saved-by: maintainer backfill (post-ADR-0014 acceptance)
Source-URL: https://github.com/libz-renlab-ai/TeamBrain/issues/218
---

# Grill log: issue #218

> Saved verbatim from GitHub comments per ADR-0014 §"Operational shape" step 3.
> Save-mode backfill: issue pre-existed before ADR-0014 was accepted (2026-05-11),
> so this file is written from existing GitHub grill comments — no new grilling.

## Issue metadata

- **Title**: [fixedflow] init 输出无 FIXEDFLOW 引导
- **State**: CLOSED
- **Closed at**: 2026-05-09T11:29:48Z
- **Closing / related PRs**: #226
- **Source URL**: https://github.com/libz-renlab-ai/TeamBrain/issues/218

## Grill comments (verbatim)

### Comment 1 — @LiuShiyuMath at 2026-05-09T11:06:22Z

# Grill — Issue #218: init 输出无 FIXEDFLOW 引导

按 `docs/HOWTO-PLAN-PR.md` 4 段结构产出。driver 把本评论当作 plan 直接实现；如有歧义，先在 `docs/plans/2026-05-09-issue-218/research.md` 记录再请示，不得自决偏离。

## 1. Plan / 任务描述

**做什么 (what)**

让 `pnpm teamagent init` 跑完后明确告诉用户 / agent：TeamBrain 唯一允许的 issue → PR → merged 路径是 FIXEDFLOW + PR-PLAN + POSTPR full chain。新增项目级 routing skill `claim-to-merge`，并在 init 流程做两件事：(a) 紧贴「✅ TeamAgent 安装成功！」打 FIXEDFLOW banner（产品特性 + 复制即用的 verify prompt + 详情链接）；(b) 把 skill 复制到用户级 `~/.claude/skills/teamagent/claim-to-merge/SKILL.md` 让全局可见。

**怎么做 (how)**

### A. 新建 project-level routing skill（双镜像）

- 文件 1：`${REPO_ROOT}/.claude/skills/claim-to-merge/SKILL.md`
- 文件 2：`${REPO_ROOT}/.codex/skills/claim-to-merge/SKILL.md`（与文件 1 内容完全一致；不要 symlink，遵循 `CLAUDE.md` 项目规则）
- 行数预算：≤ 80 行
- frontmatter:
  ```
  ---
  name: claim-to-merge
  description: TeamBrain claim-an-issue → merged-code routing。给「我要 claim 一个 issue / 想把 issue 推到合并」这类 prompt 一个统一入口表，链回 docs/FIXEDFLOW.md 等 canonical doc。Use when user says "claim issue", "走 FIXEDFLOW", "how do I get my issue merged", "issue 怎么自动变成 PR".
  ---
  ```
- 内容只准包含：
  1. 一段 ASCII art header（per AGENTS.md L37 规则）
  2. 5-step 主链一句话总结：step 1-2 manual（写 ≤50 字 issue + grill comment + grill-ready label），step 3-5 auto（mainpi 起 worktree → impl → /review fix-loop → 普通 PR → squash-merge → 清理）
  3. 2-outcome contract：无 `grill-ready` label / 无效 grill → driver pause-and-stop；条件齐备 → 全自动跑到 squash-merged，期间无人介入
  4. 入口表（每条 1 行 + 链接，**不复制内容**）：
     - `docs/FIXEDFLOW.md` — 唯一 issue→PR→merge 工作流，5 步铁律 + 2-outcome contract
     - `docs/HOWTO-PLAN-PR.md` — PR 描述 4 段结构（plan / outputs / verify / claudefast probes）
     - `.claude/skills/fixed-flow-driver/SKILL.md` — driver step 3-5 实现细则
     - `docs/PR-PLAN.md` — `/review` 出 issue 时禁开 follow-up issue，写 fix-plan，TEAMWORK 修
     - `docs/POSTPR.md` — `/review` PASS 后的 squash-only merge → ExitWorktree → ff pull
     - `docs/TEAMWORK.md` — N+1+(2N) 并行 worker 模式
     - `docs/feature-verification.md` / `docs/FASTPROBE.md` / `docs/CLAUDEFAST.md` — 验证门 + fastprobe + claudefast wrapper
  5. canned probe（让用户自验）：`claudefast -p "explain TeamBrain FIXEDFLOW: 5 steps, what's manual vs auto"`
- 严禁：复制 doc 内容（drift）、写 implementation steps（driver 已有自己的 SKILL）、写 ADR（不满足 grill-with-docs 三条件）。

### B. 扩展 init 流程：把 skill 装到用户级

修改：`packages/cli/src/commands/init.ts`

- 在 `compile-skills` 步骤之后（约 line 245 附近）新增 step `mirror-claim-to-merge-skill`：
  - 触发条件：`opts.target === "claude" || opts.target === "both"`
  - 源：`${REPO_ROOT}/.claude/skills/claim-to-merge/SKILL.md`（不存在则 skip 并记 `status: "skipped"` `detail: "source skill missing"`）
  - 目标：`${HOME}/.claude/skills/teamagent/claim-to-merge/SKILL.md`（dir 不存在则 mkdir -p）
  - 写入：直接复制源文件内容（不做模板化）
  - `--dry-run` 模式：只打印路径，不写
  - 失败不 fatal：通过 `failStep("mirror-claim-to-merge-skill", ...)` 记录但 init 继续
- 把这个 step 加进 `renderInitResult()` 的 `stepGroups` 中「📄 导出 Skills」组，与 `compile-skills` 同组显示。

### C. renderInitResult 加 FIXEDFLOW banner

修改：`packages/cli/src/commands/init.ts:1297-1371`

- 紧贴 line 1335「✅ TeamAgent 安装成功！\n」之后、line 1336「下一步:」之前，插入 banner：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🌊 FIXEDFLOW — 本仓库 issue → merged code 的唯一路径
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  产品特性
    你写 ≤50 字 issue + 贴 grill 评论 + 加 grill-ready label，本地
    mainpi 自动: worktree → 实现 → /review fix-loop（无限至 PASS）→
    普通 PR → squash-merge → 清理。Step 3-5 全程无人介入。
    /review 出 issue 时强制走 PR-PLAN（禁开 follow-up issue）；
    POSTPR 仅 squash-merge（禁 --merge / --rebase）。

  快速验证（复制运行）
    claudefast -p "explain TeamBrain FIXEDFLOW: 5 steps, what's manual vs auto"

  详情
    .claude/skills/claim-to-merge/SKILL.md (TL;DR routing)
    docs/FIXEDFLOW.md / docs/PR-PLAN.md / docs/POSTPR.md (canonical)
```

- 触发：`result.ok === true` 即打（不区分 target，Codex / Claude / both 都打）
- `--dry-run`：仍打（预览有用）
- `result.ok === false`：不打（避免在错误日志上加噪音）
- 整段 `lines.push(...)` 写入；`duckifyText()` 包装保持一致

### D. 测试

追加：`packages/cli/src/__tests__/init.test.ts`（**已存在**，line 699 起有 `describe("renderInitResult", ...)` 块；driver 把以下 3 个 case 追加进同一个 describe 块，**不要新建文件、不要新建顶层 describe**。bin.ts:504 / bin.ts:511 都把 `renderInitResult` 输出写 stdout，故 banner 在 `--target claude/codex/both` 全部命中）

3 个新 case，用 `renderInitResult()` 直接测：
- case A：`{ ok: true, dryRun: false, steps: [...] }` 输出含 `"FIXEDFLOW"`、`"claudefast -p"`、`"docs/FIXEDFLOW.md"` 三个锚点
- case B：`{ ok: false, ... }` 输出**不**含 `"FIXEDFLOW"`
- case C：`{ ok: true, dryRun: true, ... }` 输出**同时**含 `"FIXEDFLOW"` 与 `"⚠️  预览模式"`

跑 `pnpm test` 与 `pnpm typecheck` 全绿。

### 不做什么 (out of scope)

- 不向 `CLAUDE.md` / `AGENTS.md` 写 FIXEDFLOW canned-answer block（ADR-0007 / FIXEDFLOW.md L125 硬约束）
- 不重写 `docs/FIXEDFLOW.md` / `docs/PR-PLAN.md` / `docs/POSTPR.md` 任何一行
- 不动 `scripts/fixed-flow-watcher.sh`
- 不创建 ADR
- 不动其他 init step（pre-check / load-seed / scan-rules / install-hook / pack 管理 / vector warmup 等）
- 不删 `--legacy-claude-md` 行为

## 2. Expected outputs / 可验收交付物

| 类别 | 路径 | 行数预算 |
|------|------|----------|
| 新文件 | `.claude/skills/claim-to-merge/SKILL.md` | ≤ 80 |
| 新文件 | `.codex/skills/claim-to-merge/SKILL.md` | ≤ 80（与上同） |
| 修改 | `packages/cli/src/commands/init.ts`（新 step `mirror-claim-to-merge-skill` 约 line 245 区段；`renderInitResult()` banner 区段 ~1335-1336 之间） | +60 / -0 |
| 新 / 改 | `packages/cli/src/__tests__/init.test.ts` 三个新 case | +50 |
| 新文件 | `docs/plans/2026-05-09-issue-218/research.md`（Boris workflow） | ≤ 100 |
| 新文件 | `docs/plans/2026-05-09-issue-218/judge.md`（V1/V2/V3 playbook） | ≤ 80 |
| 合并后写 | `docs/plans/2026-05-09-issue-218/report.md`（Boris workflow） | ≤ 60 |

## 3. How-to-verify (third-party judge harness)

按 `docs/feature-verification.md` 三段（V1 RUN / V2 DUMP / V3 READ）+ `docs/FIXEDFLOW.md` L127 已规定的 canonical probe。

### V1 RUN — 跑固定工具

- `pnpm test --filter @teamagent/cli init.test.ts`（vitest 单元）
- `pnpm typecheck`（TS 类型门）
- `pnpm teamagent init --dry-run --target both` 在 `.judge/<run_id>/sandbox/` tmpdir 跑（不污染真实 `~/.claude/`）
- `claudefast -p "explain TeamBrain FIXEDFLOW: 5 steps, what's manual vs auto"`（FIXEDFLOW canonical probe）
- `bash scripts/verify-gstack-skill-mirrors.sh`（双镜像一致性 utility，非 judge 本身）

### V2 DUMP — 写固定 JSON

dump 到 `.judge/<run_id>/judge.json`：

```json
{
  "issue": 218,
  "exit_code": <int>,
  "metrics": {
    "vitest_pass": <bool>,
    "typecheck_pass": <bool>,
    "init_dry_run_exit": <int>,
    "stdout_contains_fixedflow_banner": <bool>,
    "stdout_contains_verify_prompt": <bool>,
    "skill_file_present": <bool>,
    "codex_mirror_present": <bool>,
    "user_skill_present_after_init": <bool>,
    "canonical_probe_hit_5_steps": <bool>,
    "canonical_probe_hit_manual_auto": <bool>,
    "mirror_consistency_pass": <bool>
  },
  "evidence_dir": ".judge/<run_id>/",
  "stdout_path": ".judge/<run_id>/init.stdout",
  "probe_path": ".judge/<run_id>/probe.txt"
}
```

evidence_dir 包含完整 stdout / stderr / probe 输出。

### V3 READ — LLM 裁判

driver 跑 `claudefast -p` 喂入 V2 JSON 路径与 evidence_dir，prompt：

```
你是 TeamBrain Issue #218 的 third-party judge。读 .judge/<run_id>/judge.json 与 evidence_dir 内容，按以下规则给 PASS / FAIL：

PASS 条件（all must hold）:
- exit_code == 0
- vitest_pass && typecheck_pass
- stdout_contains_fixedflow_banner && stdout_contains_verify_prompt
- skill_file_present && codex_mirror_present && user_skill_present_after_init
- canonical_probe_hit_5_steps && canonical_probe_hit_manual_auto
- mirror_consistency_pass

任意 false → FAIL，列出具体 false 项与对应 evidence 截取。

输出严格 JSON: {"verdict": "PASS"|"FAIL", "reasons": [...], "evidence_refs": [...]}
```

裁判 LLM 不得读源码、不得跑工具，只读 JSON + evidence 文件。判罚锚点写到 `docs/plans/2026-05-09-issue-218/judge.md`。

## 4. claudefast probes（per FASTPROBE.md）

并行 probe ≤ 8 路，记到 `.fastprobe/`：

- `claudefast -p "explain TeamBrain FIXEDFLOW: 5 steps, what's manual vs auto"` — 必须命中 5 步 + manual/auto 切分
- `claudefast -p "what does pnpm teamagent init print at the end about FIXEDFLOW?"` — 必须命中 banner 内容（含 verify prompt 字面）
- `claudefast -p "what file holds the claim-to-merge skill in this repo?"` — 必须命中 `.claude/skills/claim-to-merge/SKILL.md` 与 `.codex/skills/claim-to-merge/SKILL.md`
- `claudefast -p "after pnpm teamagent init runs, where is claim-to-merge skill installed user-level?"` — 必须命中 `~/.claude/skills/teamagent/claim-to-merge/SKILL.md`
- `claudefast -p "what is TeamBrain's PR-PLAN protocol when /review finds issues?"` — 命中：禁开 follow-up issue / 写 fix-plan / TEAMWORK 修 / 同 PR branch
- `claudefast -p "what's the canonical merge command in TeamBrain POSTPR?"` — 命中 `gh pr merge <N> --squash`，不许 `--merge` / `--rebase`

probe 全过 + V3 LLM 裁判 PASS → `/review` 进入 PR-PLAN / POSTPR 收尾流程。

---

driver: 严格按本 grill 实现；任何偏离请在 `docs/plans/2026-05-09-issue-218/research.md` 记录 deviation 并在 PR description 写明理由。

--- end grill ---


---


<!-- end grill log -->
