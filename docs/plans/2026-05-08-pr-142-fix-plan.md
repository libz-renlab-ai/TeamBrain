```text
   ┌────────────────────────────────────────────────────┐
   │  PR #142 opus reviewer findings                    │
   │     ↓                                              │
   │  4 fixes (1 critical + 3 info)                     │
   │     ↓                                              │
   │  Worker 1 sonnet ──── 2 claudefast probes          │
   │     ↓                                              │
   │  Reporter opus ──── judge harness                  │
   │     ↓                                              │
   │  PASS → push same PR branch → merge ready          │
   └────────────────────────────────────────────────────┘
```

# PR-PLAN: PR #142 fix from opus adversarial review

**Date**: 2026-05-08
**PR**: #142 — refactor(arch): archive 6 hypothetical port seams (M0)
**Branch**: `teamwork/archive-hypothetical-ports`
**Source**: opus 1M adversarial reviewer (Codex was rate-limited)
**Authority**: CLAUDE.md POSTPR rule — P1/P2 must fix in same PR before merge

---

## 1. Task description

### 做什么

修 opus reviewer 在 PR #142 上发现的 4 个 finding：

1. **(P1, CRITICAL, conf 9/10)** Dedupe `RawErrorSignal` — 当前 inline 在两处（`packages/core/src/error-collector/cross-session-cluster.ts` + `packages/adapters/src/error-collector/composite-error-signal-collector.ts`），违反 single-source-of-truth 原则。靠 TS structural typing 临时 work，未来字段变化会 silent break。
2. **(P2, INFO, conf 8/10)** 把 `import` 移到文件顶部 — `composite-error-signal-collector.ts:35` 和 `fs-team-rule-store.ts:38` 上 import 在 export 之后，layout 乱。
3. **(P3, INFO, conf 7/10)** 给 4 个 archived contract test 加 frozen-historical 头注 — `_archived/__tests__/*-contract.ts` 仍 import 旧 archived port file（路径解析 OK，但语义悬空）。
4. **(P3, INFO, conf 6/10)** 修 `_archived/README.md` 第 13 行 typo — `error-signal-collector` 行说 "lone SQLite adapter"，实际是 `CompositeErrorSignalCollector`。

### 怎么做（按 finding）

#### Fix 1: Dedupe RawErrorSignal (single commit)

**Decision**: 保留 `core/src/error-collector/cross-session-cluster.ts:2-N` 中的 `RawErrorSignal` 定义（因为 4 个 core/error-collector/* 文件都 import 它）；从 `adapters/src/error-collector/composite-error-signal-collector.ts` 删除重复定义，改为 `import type { RawErrorSignal } from "@teamagent/core"`。

操作：
- `packages/adapters/src/error-collector/composite-error-signal-collector.ts`：删除 inlined `interface RawErrorSignal`，加 `import type { RawErrorSignal } from "@teamagent/core"` 到 import block。**保留** `interface ErrorSignalCollector` 和 `class CompositeErrorSignalCollector`（这两个是 adapters 自己的）。
- `packages/adapters/src/index.ts`：删除 `RawErrorSignal` 的 re-export（保留 `ErrorSignalCollector` re-export）。
- `packages/adapters/src/error-collector/__tests__/composite-error-signal-collector.test.ts`：如果 import `RawErrorSignal` from adapters 自己，改 from `@teamagent/core`。
- 验证：`grep -rn "export interface RawErrorSignal\b" packages/` 只返回 1 行。

#### Fix 2: Reorder imports

- `packages/adapters/src/error-collector/composite-error-signal-collector.ts`: 把 line ~35 的 `import { ruleBasedCorrectionDetector, clusterByTag } from "@teamagent/core"` 移到 line 1-3 区块。
- `packages/adapters/src/m5/fs-team-rule-store.ts`: 把 line ~38 的 `import { parseTeamRule, serializeTeamRule } from "@teamagent/core"` 移到顶部。

#### Fix 3: Frozen-historical note in archived contracts

每个 `_archived/__tests__/{candidate-queue,error-signal-collector,bootstrap-port,team-rule-store-port}-contract.ts` 文件顶部加：

```ts
/**
 * ⚠️ ARCHIVED CONTRACT — frozen historical artifact.
 *
 * 见 docs/adr/0005-archive-hypothetical-port-seams.md。
 * 此文件不再被 CI 跑，但保留以便未来 ≥2 production adapter
 * 出现时可参考原 contract 设计。
 *
 * 不要在这里加新 case；如果当前 lone implementation
 * 演化出 contract 偏差，请直接编辑 lone-impl 的 unit test，
 * 不要"复活"这个 contract。
 */
```

#### Fix 4: README typo

`packages/ports/src/_archived/README.md` 第 13 行 `error-signal-collector.ts` 行的 "Same as above" 改为：
```
| `error-signal-collector.ts` | `CompositeErrorSignalCollector` @ `packages/adapters/src/error-collector/composite-error-signal-collector.ts` | Contract test exists but only ever validated against the lone Composite adapter |
```

### 不做什么

- ❌ 不重排其他文件 import order（只修 reviewer 指出的 2 个）
- ❌ 不动 unrelated CONTEXT.md / ADR-0005（这两份在 b42eae8 已 commit）
- ❌ 不动 hookshell stash（仍隔离）
- ❌ 不开 follow-up issue / follow-up PR — 全部 inline 修在本 PR

---

## 2. Expected outputs

| # | 交付物 | 路径 / 标识 | 验收 |
|---|---|---|---|
| 1 | `RawErrorSignal` 单一定义 | `grep -c "export interface RawErrorSignal\b" packages/{core,adapters}` | 输出 `1` |
| 2 | adapters/index.ts 不再 re-export RawErrorSignal | `grep -c "RawErrorSignal" packages/adapters/src/index.ts` | 输出 `0` |
| 3 | composite-error-signal-collector.ts imports 全部在文件顶部 | 第一个非 import / 非注释 / 非 blank 行的行号 ≥ 所有 import 行号 | manual review |
| 4 | fs-team-rule-store.ts imports 同上 | 同上 | manual review |
| 5 | 4 个 archived contract test 含 frozen-historical 头注 | `grep -l "ARCHIVED CONTRACT" packages/ports/src/_archived/__tests__/*.ts \| wc -l` | 输出 `4` |
| 6 | README typo 修复 | `grep -A1 "error-signal-collector.ts" packages/ports/src/_archived/README.md \| grep "Composite adapter"` | hit |
| 7 | `pnpm typecheck` | exit code | 0 |
| 8 | `pnpm test` | exit code | 0 |
| 9 | `pnpm teamagent skeleton-demo` | exit code | 0 |
| 10 | `pnpm teamagent m5-sync --dry-run` | exit code | 0 |
| 11 | git commit on `teamwork/archive-hypothetical-ports` | `git log -1 --pretty=%s` | `fix(arch): address opus review findings on PR #142` |
| 12 | git push 同 PR branch | `git rev-parse origin/teamwork/archive-hypothetical-ports` | 等于 HEAD |

---

## 3. How-to-eval-from-3rd-party-harness

### Judge harness playbook

(per `feedback_judge_harness_md_playbook.md` — MAIN agent dispatch via subagents / `claudefast -p` probes，不是 fixed bash script)

### RUN — opus reporter dispatched probes

每个 probe 产 evidence to `.judge/<run_id>/<probe>/`，run_id = `pr-142-fix-$(date +%s)`：

1. **typecheck**: `pnpm typecheck` → `.judge/.../typecheck/{stdout,exit_code}`. Expected exit 0.
2. **test**: `pnpm test --reporter=json` → `.judge/.../vitest/output.json`. Expected exit 0.
3. **skeleton**: `pnpm teamagent skeleton-demo` → `.judge/.../skeleton/stdout`. Expected exit 0.
4. **m5-sync**: `pnpm teamagent m5-sync --dry-run` → `.judge/.../m5/stdout`. Expected exit 0.
5. **dedup grep**: `grep -rn "^export interface RawErrorSignal\b" packages/{core,adapters}/src/` → expected exactly 1 line, in `packages/core/src/error-collector/cross-session-cluster.ts` (or wherever lone-source ended up).
6. **adapters reexport check**: `grep -n "RawErrorSignal" packages/adapters/src/index.ts` → expected 0 lines.
7. **import-at-top check**: read first 50 lines of `packages/adapters/src/error-collector/composite-error-signal-collector.ts` and `packages/adapters/src/m5/fs-team-rule-store.ts`. The first non-import / non-comment / non-blank statement after line 1 must be `export interface ...` or similar **after** all imports complete. Reporter inspects manually.
8. **frozen-note presence**: `grep -l "ARCHIVED CONTRACT" packages/ports/src/_archived/__tests__/*.ts | wc -l` → expected 4.
9. **README typo fixed**: `grep -B1 "Composite adapter" packages/ports/src/_archived/README.md | grep "error-signal-collector.ts"` → expected hit.

### DUMP — write `.judge/<run_id>/judge.json`

```json
{
  "run_id": "<unix epoch>",
  "plan": "2026-05-08-pr-142-fix-plan",
  "verdict": "PASS|FAIL",
  "probes": {
    "typecheck": { "exit_code": 0 },
    "test": { "exit_code": 0 },
    "skeleton": { "exit_code": 0 },
    "m5_sync": { "exit_code": 0 },
    "dedup_grep_count": { "metric": 1, "expected": 1 },
    "adapters_reexport_count": { "metric": 0, "expected": 0 },
    "imports_at_top_composite": { "verdict": "PASS|FAIL" },
    "imports_at_top_team_rule_store": { "verdict": "PASS|FAIL" },
    "frozen_note_count": { "metric": 4, "expected": 4 },
    "readme_typo_fixed": { "verdict": "PASS|FAIL" }
  },
  "failures": []
}
```

### READ — opus reporter judges

Reporter 只读 `.judge/<run_id>/judge.json` + 必要 evidence 文件。不读 worker 报告自评。

**FAIL 条件**：
- 任一 exit_code != 0
- dedup_grep_count != 1
- adapters_reexport_count != 0
- imports_at_top_* = FAIL
- frozen_note_count != 4
- readme_typo_fixed = FAIL

### 禁止
- ❌ 不让 worker 自评 PASS
- ❌ 不让 worker 跑 typecheck/test（reporter 跑）
- ❌ 不 skip probe

---

## 4. 风险 & 回滚

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| import path 改后某个 caller 漏 catch | 低 | 中 | reporter 跑 typecheck + test catch |
| 4 个 archived contract test 加注释后 vitest 错挑这个文件 | 低 | 低 | archived 不在 vitest pattern；commit 后跑 test 验证 |
| RawErrorSignal 在第三方 generic constraint 中 | 极低 | 低 | repo 内 zero 第三方 import (monorepo) |

回滚：单 commit，`git revert <sha>` 即可。

---

## 5. 依赖

- 无新外部依赖
- 不需要新环境变量
- 不需要 user permission（仅 source refactor）
