{
  "name": "@teamagent/landing",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/. dist/ && cp -r public/. dist/ && test -f dist/index.html && test -f dist/double-moment.gif && test -f dist/double-moment-static.png",
    "preview": "npx serve dist",
    "lint": "htmlhint src/",
    "verify": "lhci autorun --config=./lighthouserc.json"
  },
  "devDependencies": {
    "htmlhint": "^1.1.4",
    "serve": "^14.2.4",
    "@lhci/cli": "^0.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
