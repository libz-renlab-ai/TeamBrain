---
Issue: "#261"
Title: [fixedflow] 重写 audit/feature-10-install-plugins 到 PR #255 后的 6-plugin bundle 
State: CLOSED
Closed-at: 2026-05-10T03:44:57Z
Closing-PRs: #262
Saved-at: 2026-05-12
Saved-by: maintainer backfill (post-ADR-0014 acceptance)
Source-URL: https://github.com/libz-renlab-ai/TeamBrain/issues/261
---

# Grill log: issue #261

> Saved verbatim from GitHub comments per ADR-0014 §"Operational shape" step 3.
> Save-mode backfill: issue pre-existed before ADR-0014 was accepted (2026-05-11),
> so this file is written from existing GitHub grill comments — no new grilling.

## Issue metadata

- **Title**: [fixedflow] 重写 audit/feature-10-install-plugins 到 PR #255 后的 6-plugin bundle
- **State**: CLOSED
- **Closed at**: 2026-05-10T03:44:57Z
- **Closing / related PRs**: #262
- **Source URL**: https://github.com/libz-renlab-ai/TeamBrain/issues/261

## Grill comments (verbatim)

### Comment 1 — @LiuShiyuMath at 2026-05-10T03:39:02Z

# Grill — Issue #260: 把 audit/feature-10-install-plugins 重写到 PR #255 后的 6-plugin bundle

按 `docs/HOWTO-PLAN-PR.md` 4 段。

## 1. Plan / 任务描述

`audit/runners/feature-10-install-plugins.ts` 与 `audit/plans/feature-10-install-plugins.md` 是 PR #255 之前写的硬编码 audit harness：测的是 OLD bundle（`superpowers + sales + caveman` 跨 `claude-plugins-official + knowledge-work-plugins + JuliusBrussee/caveman` 三个 marketplace）。PR #255 后 `DEFAULT_PLUGINS` 已收敛到 6-plugin / 1-marketplace（playground / claude-code-setup / code-review / code-simplifier / commit-commands / frontend-design 全部来自 `claude-plugins-official`）。Audit 现在跑会断；NOT invoked from CI（已 verified by `grep -rn "feature-10" .github scripts package.json`）。

**做什么 (what)** — 重写两个文件让 audit 重新匹配 current `DEFAULT_PLUGINS`：

### A. `audit/runners/feature-10-install-plugins.ts` — SCENARIOS array

4 个 scenarios，按 PR #255 后的 contract 重写：

| scenario | args | expectedExit | expectedArgv | stdoutIncludes | stdoutExcludes |
|---|---|---:|---|---|---|
| `default` | `[]` | 0 | 1 marketplace add (`anthropics/claude-plugins-official`) + 6 plugin installs (playground, claude-code-setup, code-review, code-simplifier, commit-commands, frontend-design)，全部 `@claude-plugins-official` | `["Marketplaces:", "Plugins:", "7 新装", "重启 Claude Code"]` | — |
| `dry-run-project-scope` | `["--dry-run", "--scope=project"]` | 0 | `[]` (没真调) | `["预览模式", "dry-run", "7 将执行"]` | — |
| `only-code-review-local-scope` (rename from `only-sales-local-scope`) | `["--only=code-review", "--scope=local"]` | 0 | 1 marketplace add (`anthropics/claude-plugins-official`) + 1 plugin install `code-review@claude-plugins-official --scope local` | `["code-review@claude-plugins-official", "2 新装"]` | `["playground@claude-plugins-official", "frontend-design@claude-plugins-official"]` |
| `unknown-plugin` | `["--only=ghost"]` | 1 | `[]` | `["unknown plugin", "ghost", "1 失败"]` | — |

`FEATURE` 常量、`fakeClaudeScript()`、`validatorScript()`、IO 框架不动。只重写 `SCENARIOS`。

### B. `audit/plans/feature-10-install-plugins.md` — 484 行 audit 设计文档

更新 30 处 stale-bundle hits 让 doc 与 .ts 重新一致：

- L40-46 marketplaces JSON：从 2 个 marketplace + 3 plugins → 1 marketplace + 6 plugins
- L146-150 / L183-187 / L266-267 例子 argv block → 6 plugins / 1 marketplace
- L266+ scope-test argv block → 6 plugins
- L276 "### C. `--only=caveman`" 节标题 → "### C. `--only=code-review`"
- L298-309 该节正文 argv + grep 断言 → code-review
- L317-325 "### D. `--only=superpowers,playground`" → "### D. `--only=playground,code-review`"
- L342 user-scope argv → 当前 plugin
- L425-461 "### E. `--only=caveman,ghost`" → "### E. `--only=code-review,ghost`"

`### A`/`### B` (default + dry-run) 节的叙述层基本不动，只把 plugin 名替换。其他流程图、`HOME/PATH 隔离`、validator 设计等 invariant 部分保持。

### 不做什么 (out of scope)

- 不改其他 14 个 audit feature（feature-01 / 02 / ... / 20）
- 不改 `audit/runners/lib.ts`、`audit/runners/run-all.ts`
- 不修 `fakeClaudeScript()` / `validatorScript()` 的 IO 与断言框架（只改 SCENARIOS data）
- 不开 follow-up issue（PR-PLAN）

## 2. Expected outputs

| 类别 | 路径 | 备注 |
|---|---|---|
| 修改 | `audit/runners/feature-10-install-plugins.ts` | SCENARIOS array 全量替换；其余不动 |
| 修改 | `audit/plans/feature-10-install-plugins.md` | 30 处 stale-bundle hits → 6-plugin bundle |
| 新文件 | `docs/plans/2026-05-10-issue-260/research.md` | ≤ 80 行 |
| 新文件 | `docs/plans/2026-05-10-issue-260/judge.md` | ≤ 100 行 |

## 3. How-to-verify (judge harness)

V1 RUN — fixed tools:

```bash
WT=/Users/m1/projects/TeamBrain/.codex/worktrees/issue-260
RUN_DIR=$WT/.judge/2026-05-10-issue-260
mkdir -p "$RUN_DIR/raw"; cd "$WT"

# 1. residual scan in audit/feature-10 must be empty
grep -nE "(superpowers|caveman|sales|knowledge-work-plugins|JuliusBrussee)" audit/runners/feature-10-install-plugins.ts audit/plans/feature-10-install-plugins.md > "$RUN_DIR/raw/01-residual.out" 2>&1; echo $? > "$RUN_DIR/raw/01-residual.exit"

# 2. typecheck
pnpm typecheck > "$RUN_DIR/raw/02-typecheck.out" 2> "$RUN_DIR/raw/02-typecheck.err"; echo $? > "$RUN_DIR/raw/02-typecheck.exit"

# 3. audit runner: actually run feature-10 and confirm it reaches PASS verdict
pnpm exec tsx audit/runners/feature-10-install-plugins.ts > "$RUN_DIR/raw/03-audit-run.out" 2> "$RUN_DIR/raw/03-audit-run.err"; echo $? > "$RUN_DIR/raw/03-audit-run.exit"

# 4. v1.5 probe
( cd /tmp && claudefast -p "I am asking about /Users/m1/projects/TeamBrain. Run cd /Users/m1/projects/TeamBrain && grep -rln 'superpowers' --include='*.ts' --include='*.md' --include='*.json' . 2>/dev/null | grep -v node_modules | grep -v '.git/' | grep -v '.claude/worktrees/' | grep -v '.codex/worktrees/' | sort. Return markdown list. Skip docs/superpowers/ paths." --permission-mode acceptEdits ) > "$RUN_DIR/raw/04-probe.out" 2> "$RUN_DIR/raw/04-probe.err"; echo $? > "$RUN_DIR/raw/04-probe.exit"
```

V2 DUMP — `judge.json`:

```json
{
  "issue": 260,
  "metrics": {
    "audit_residual_clean": "<bool: step 1 stdout empty>",
    "typecheck_passed": "<bool: step 2 exit==0>",
    "audit_runner_passes": "<bool: step 3 exit==0 and stdout contains 'PASS' / no FAIL>",
    "probe_no_audit_files": "<bool: step 4 stdout does NOT list audit/runners/feature-10-install-plugins.ts or audit/plans/feature-10-install-plugins.md>"
  }
}
```

V3 READ — LLM judge: `claudefast -p "Read .judge/2026-05-10-issue-260/judge.json + raw/. Return PASS/FAIL. PASS iff all 4 metrics true."`

## 4. claudefast probes (post-merge)

```bash
cd /tmp
claudefast -p "in THIS project, what files we have related to superpowers ?" --permission-mode acceptEdits
```

Expected: probe stdout does NOT list `audit/runners/feature-10-install-plugins.ts` or `audit/plans/feature-10-install-plugins.md` as plugin-superpowers-related.

--- end grill ---


---


<!-- end grill log -->
