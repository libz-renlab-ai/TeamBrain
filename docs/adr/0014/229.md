---
Issue: "#229"
Title: [fixedflow] remove mainpi watcher + auto-dispatch from step 3-5 
State: CLOSED
Closed-at: 2026-05-09T11:48:36Z
Closing-PRs: #231, #236
Saved-at: 2026-05-12
Saved-by: maintainer backfill (post-ADR-0014 acceptance)
Source-URL: https://github.com/libz-renlab-ai/TeamBrain/issues/229
---

# Grill log: issue #229

> Saved verbatim from GitHub comments per ADR-0014 §"Operational shape" step 3.
> Save-mode backfill: issue pre-existed before ADR-0014 was accepted (2026-05-11),
> so this file is written from existing GitHub grill comments — no new grilling.

## Issue metadata

- **Title**: [fixedflow] remove mainpi watcher + auto-dispatch from step 3-5
- **State**: CLOSED
- **Closed at**: 2026-05-09T11:48:36Z
- **Closing / related PRs**: #231, #236
- **Source URL**: https://github.com/libz-renlab-ai/TeamBrain/issues/229

## Grill comments (verbatim)

### Comment 1 — @LiuShiyuMath at 2026-05-09T11:40:52Z

## 取证发现

PR #200 (`f7e0e16`, 2026-05-09 13:19) 由 LiuShiyuMath / liusy-renlab-ai (同一人，两个 CI 别名) 提交，落地 1287 行 FIXEDFLOW 系统。Codex + claudefast 双审都 rate-limit 失败 → 自审自合并，无 ADR。mainpi orchestrator 依赖在 `docs/plans/2026-05-09-fixed-flow/research.md:96-110` 被当作「现有模式复用」直接采纳，无 decision-record。

实际运行时 mainpi 调用唯一一处：`scripts/fixed-flow-watcher.sh:84-97` 的 `dispatch_mainpi()`。当前 gated (`FIXEDFLOW_DRIVER_ENABLED=0` 默认)，从未真跑过。

## 决策

Maintainer 指令：**杀掉所有 watcher / 后台轮询 / 自动 dispatch 概念**。FIXEDFLOW step 3-5 只能由 maintainer 在 Claude Code 会话里手动调用 `/fixed-flow-driver` skill 触发。

## 实现计划

1. **删除** `scripts/fixed-flow-watcher.sh`（mainpi 唯一调用方，150 行）
2. **删除** `.github/workflows/fixed-flow-heartbeat.yml`（依赖 watcher 心跳，51 行）
3. **重写** `docs/FIXEDFLOW.md` step 3-5 从 auto 到 manual；显式禁止 watcher；删除「未来 phase 2」段落（迁移到云端 watcher）；删除「30 秒内」SLA；删除 Mac-off escape-hatch 行
4. **清除** mainpi/watcher 在活文档：`CLAUDE.md`、`docs/PROJECT-TOOLS.md`、`.claude/skills/fixed-flow-driver/SKILL.md`、`.codex/skills/fixed-flow-driver/SKILL.md`、`.github/ISSUE_TEMPLATE/fixed-flow.yml`
5. **保留** `docs/plans/2026-05-09-fixed-flow/*.md` 与 `docs/plans/2026-05-09-pr-200-fix-plan.md` 作为审计留痕（不篡改历史）

## 预期产出

- 活文档（非 historical）surface 0 处 mainpi 提及
- 0 处 `scripts/fixed-flow-watcher.sh` 引用
- `docs/FIXEDFLOW.md` 显式写「禁止任何 watcher / 后台轮询 / 自动 dispatch」
- `/fixed-flow-driver` skill description 写「Invoked manually by a maintainer」
- 5 个 historical plan 文件未动

## 第三方 judge harness

实现后跑：`claudefast -p "explain TeamBrain FIXEDFLOW: 5 steps, who triggers step 3"`。

PASS anchors：
1. 输出提到「maintainer」「人手」或「手动」作为 step 3 触发者
2. 输出**不**提到「mainpi」「watcher」「30 秒」「auto-dispatch」作为活跃机制
3. 输出提到 `/fixed-flow-driver` skill 作为手动入口

LLM judge 读原始 stdout + 与 `snapshots/fixedflow.canonical.txt` 对照（合并后重新生成）。

## Out of scope

- 迁移到 GitHub Actions claude-code-action（已删除的 phase 2 提到——明确**不**选，因为 maintainer 不要任何自动 trigger）
- `~/.pi/agent/docs/mainpi.md`（用户级，不在仓库内）
- Historical `docs/plans/2026-05-09-fixed-flow/*.md`（审计留痕保留）

--- end grill ---


---


<!-- end grill log -->
