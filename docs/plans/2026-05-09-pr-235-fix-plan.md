# PR-PLAN — PR #235 /review fix iteration 1

```
       __
     >( o )>     呷呷~ /review iter-1
      \   /     adversarial findings → AUTO-FIX
       \ /
       ^^
   ┌─────────────────────────────────────┐
   │  Claude adv subagent: 4 INFORMATIONAL│
   │  Codex adv: unavailable (auth 401)   │
   │  AUTO-FIX: 2 / SKIP: 2 (by design)  │
   └─────────────────────────────────────┘
```

> Per `docs/PR-PLAN.md` + `docs/FIXEDFLOW.md` step 4 mandate: every /review
> fix iteration writes a three-section PR-PLAN with task / outputs / judge
> harness. This is iter-1 for PR #235.

## 1. Task

Apply 2 mechanical fixes from the Claude adversarial subagent review of
`.claude/hooks/newsboard-session-start.sh`:

1. **CRLF strip in awk** (confidence 8/10) — Windows checkouts with
   `core.autocrlf=true` inject `\r` into `docs/PRODUCT-FEATURES.md` reads,
   producing a stray carriage return in section [4]'s output. Add
   `sub(/\r$/, "")` inside the awk block.
2. **`-E` flag for git log grep** (confidence 7/10) — replace BRE alternation
   `--grep='^feat\|^fix'` with ERE `-E --grep='^(feat|fix)'`. Equivalent
   matching, less ambiguous.

**Skip (by design):**

- Section 2 mislabel (confidence 6/10) — plan.md explicitly states "demo,
  no telemetry"; the static `"haven't tried? give it a try"` label is the
  documented design choice. Filed as known cosmetic.
- Modulo determinism (confidence 5/10) — same-second sessions pick same
  feature. Cosmetic on a banner; "random" is aspirational, not crypto-grade.

## 2. Expected outputs

| Path | Change |
|---|---|
| `.claude/hooks/newsboard-session-start.sh` | 2 line edits (awk + grep) |
| `docs/plans/2026-05-09-pr-235-fix-plan.md` | this file (new) |

No new commits to plan/research/judge docs (their content is unchanged).

## 3. How to verify (judge harness)

Re-run `docs/plans/2026-05-09-newsboard-session-start/judge.md`
RUN→DUMP→READ blocks against the post-fix hook. Expect:

- All 6 assertions PASS (A1 exit=0, A2 stdout=0 bytes, A3 header literal,
  A4 4 labels, A5 ≥1 PRODUCT-FEATURES.md feature in stderr, A6 <500ms)
- `OVERALL: PASS` from independent claudefast LLM-judge
- duration_ms remains comparable (was 177ms post-fix vs 122-165ms baseline)

**Re-run captured 2026-05-09 ~20:06 UTC: `OVERALL: PASS`** — see
`.judge/20260509-200639-postfix/judge.json` for the canonical run record.
